// Generated by OABuilder
package com.cdi.model.filter;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.annotation.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.filter.*;
import com.cdi.model.search.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class UserActiveFilterModel {
    private static Logger LOG = Logger.getLogger(UserActiveFilterModel.class.getName());
    
    // Hubs
    protected Hub<UserActiveFilter> hubFilter;
    
    // ObjectModels
    
    // object used for filter data
    protected UserActiveFilter filter;
    
    public UserActiveFilterModel(Hub<User> hubMaster, Hub<User> hub) {
        filter = new UserActiveFilter(hubMaster, hub);
    }
    public UserActiveFilterModel(Hub<User> hub) {
        filter = new UserActiveFilter(hub);
    }
    
    // object used to input query data, to be used by filterHub
    public UserActiveFilter getFilter() {
        return filter;
    }
    
    // hub for filter UI object - used to bind with UI components for entering filter data
    public Hub<UserActiveFilter> getFilterHub() {
        if (hubFilter == null) {
            hubFilter = new Hub<UserActiveFilter>(UserActiveFilter.class);
            hubFilter.add(getFilter());
            hubFilter.setPos(0);
        }
        return hubFilter;
    }
    
    
    
    // get the Filtered hub
    public Hub<User> getHub() {
        return getFilter().getHubFilter().getHub();
    }
}

