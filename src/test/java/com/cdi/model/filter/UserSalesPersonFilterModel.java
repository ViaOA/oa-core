// Generated by OABuilder
package com.cdi.model.filter;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.annotation.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.ds.*;

import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.filter.*;
import com.cdi.model.search.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class UserSalesPersonFilterModel {
    private static Logger LOG = Logger.getLogger(UserSalesPersonFilterModel.class.getName());
    
    // Hubs
    protected Hub<UserSalesPersonFilter> hubFilter;
    
    // ObjectModels
    
    // object used for filter data
    protected UserSalesPersonFilter filter;
    
    public UserSalesPersonFilterModel(Hub<User> hubMaster, Hub<User> hub) {
        filter = new UserSalesPersonFilter(hubMaster, hub);
    }
    public UserSalesPersonFilterModel(Hub<User> hub) {
        filter = new UserSalesPersonFilter(hub);
    }
    
    // object used to input query data, to be used by filterHub
    public UserSalesPersonFilter getFilter() {
        return filter;
    }
    
    // hub for filter UI object - used to bind with UI components for entering filter data
    public Hub<UserSalesPersonFilter> getFilterHub() {
        if (hubFilter == null) {
            hubFilter = new Hub<UserSalesPersonFilter>(UserSalesPersonFilter.class);
            hubFilter.add(getFilter());
            hubFilter.setPos(0);
        }
        return hubFilter;
    }
    
    
    
    // get the Filtered hub
    public Hub<User> getHub() {
        return getFilter().getHubFilter().getHub();
    }
}

