// Generated by OABuilder
package com.cdi.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.util.*;
import javax.swing.JLabel;
import com.viaoa.converter.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.annotation.*;
import com.cdi.model.oa.method.*;
import com.cdi.delegate.oa.*;
import com.cdi.model.oa.filter.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.delegate.ModelDelegate;
 
@OAClass(
    lowerName = "salesOrderItem",
    pluralName = "SalesOrderItems",
    shortName = "soi",
    displayName = "Sales Order Item",
    displayProperty = "salesOrder",
    sortProperty = "salesOrder",
    noPojo = true
)
@OATable(
    indexes = {
        @OAIndex(name = "NewOrderItemItem", fkey = true, columns = { @OAIndexColumn(name = "ItemId") }), 
        @OAIndex(name = "NewOrderItemSalesOrder", fkey = true, columns = { @OAIndexColumn(name = "SalesOrderId") }), 
        @OAIndex(name = "SalesOrderItemServiceCode", fkey = true, columns = { @OAIndexColumn(name = "ServiceCodeId") })
    }
)
public class SalesOrderItem extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(SalesOrderItem.class.getName());

    public static final String P_Id = "id";
    public static final String P_Seq = "seq";
    public static final String P_Location = "location";
    public static final String P_CustomItem = "customItem";
    public static final String P_ItemDescription = "itemDescription";
    public static final String P_Quantity = "quantity";
    public static final String P_UnitPrice = "unitPrice";
    public static final String P_UnitWeight = "unitWeight";
    public static final String P_ServicePrice = "servicePrice";
    public static final String P_AddOnPrice = "addOnPrice";
    public static final String P_Note = "note";
    public static final String P_ShowSubTotal = "showSubTotal";
    public static final String P_FakeUnitPrice = "fakeUnitPrice";
     
    public static final String P_SellRate = "sellRate";
    public static final String P_TotalItemOurPrice = "totalItemOurPrice";
    public static final String P_ItemSellPrice = "itemSellPrice";
    public static final String P_TotalItemSellPrice = "totalItemSellPrice";
    public static final String P_TotalSellPrice = "totalSellPrice";
    public static final String P_TotalWeight = "totalWeight";
    public static final String P_AllowsItemAddOn = "allowsItemAddOn";
     
    public static final String P_Item = "item";
    public static final String P_ItemId = "itemId"; // fkey
    public static final String P_ItemAddOns = "itemAddOns";
    public static final String P_ItemAddOnsId = "itemAddOnsId"; // fkey
    public static final String P_SalesOrder = "salesOrder";
    public static final String P_SalesOrderId = "salesOrderId"; // fkey
    public static final String P_ServiceCode = "serviceCode";
    public static final String P_ServiceCodeId = "serviceCodeId"; // fkey
     
    public static final String M_AssignSameItemAddOn = "assignSameItemAddOn";
    protected volatile int id;
    protected volatile int seq;
    protected volatile String location;
    protected volatile boolean customItem;
    protected volatile String itemDescription;
    protected volatile int quantity;
    protected volatile double unitPrice;
    protected volatile int unitWeight;
    protected volatile double servicePrice;
    protected volatile double addOnPrice;
    protected volatile String note;
    protected volatile boolean showSubTotal;
    protected volatile double fakeUnitPrice;
     
    // Links to other objects.
    protected volatile transient Item item;
    protected transient Hub<ItemAddOn> hubItemAddOns;
    protected volatile transient SalesOrder salesOrder;
    protected volatile transient ServiceCode serviceCode;
     
    public SalesOrderItem() {
        if (!isLoading()) setObjectDefaults();
    }
     
    public SalesOrderItem(int id) {
        this();
        setId(id);
    }
    @OAObjCallback(supportedTypes = {OAObjectCallback.Type.AfterCopy})
    public void callback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case AfterCopy:
            // todo: custom code here;
            callback.ack();
            SalesOrderItem salesOrderItemCopy = (SalesOrderItem) callback.getValue();
            salesOrderItemCopy.getItemAddOns().clear();
            salesOrderItemCopy.setAddOnPrice(0.0d);
            for (ItemAddOn ao : getItemAddOns()) {
                salesOrderItemCopy.getItemAddOns().add(ao);
            }
            break;
        }
    }

    @OAProperty(lowerName = "id", isUnique = true, displayLength = 6)
    @OAId
    @OAColumn(name = "Id", sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }

    @OAProperty(lowerName = "seq", trackPrimitiveNull = false, displayLength = 6, isAutoSeq = true)
    @OAColumn(name = "Seq", sqlType = java.sql.Types.INTEGER)
    public int getSeq() {
        return seq;
    }
    public void setSeq(int newValue) {
        int old = seq;
        fireBeforePropertyChange(P_Seq, old, newValue);
        this.seq = newValue;
        firePropertyChange(P_Seq, old, this.seq);
    }

    @OAProperty(lowerName = "location", maxLength = 75, displayLength = 10)
    @OAColumn(name = "Location", maxLength = 75)
    public String getLocation() {
        return location;
    }
    public void setLocation(String newValue) {
        String old = location;
        fireBeforePropertyChange(P_Location, old, newValue);
        this.location = newValue;
        firePropertyChange(P_Location, old, this.location);
    }

    @OAProperty(lowerName = "customItem", displayName = "Custom Item", displayLength = 5, uiColumnLength = 6, format = "true;false;false", uiColumnName = "Custom", hasCustomCode = true)
    @OAColumn(name = "CustomItem", sqlType = java.sql.Types.BOOLEAN)
    public boolean getCustomItem() {
        return customItem;
    }
    public void setCustomItem(boolean newValue) {
        fireBeforePropertyChange(P_CustomItem, this.customItem, newValue);
        boolean old = customItem;
        this.customItem = newValue;
        firePropertyChange(P_CustomItem, old, this.customItem);
    
        if (isLoading()) return;
        if (!beginServerOnly()) return;
        try {
            setItem(null);
            setItemDescription(null);
            setUnitPrice(0d);
            setNull(P_UnitPrice);
            if (!customItem && getItem() == null) getItemAddOns().removeAll();
        }
        finally {
            endServerOnly();
        }
    }
     
    @OAObjCallback
    public void customItemCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case SetConfirmForPropertyChange:
            // todo: custom code here;
            callback.ack();
            if (callback.getBooleanValue()) {
                if (getItem() != null) {
                    callback.setConfirmMessage("Ok to change to custom item and remove the current item?");
                }
            }
            else if (OAString.isNotEmpty(getItemDescription())) {
                callback.setConfirmMessage("Ok to change from custom item and remove the current item description?");
            } 
           break;
        }
    }

    @OAProperty(lowerName = "itemDescription", displayName = "Item Description", maxLength = 75, displayLength = 20, uiColumnLength = 15, hasCustomCode = true)
    @OAColumn(name = "ItemDescription", maxLength = 75)
    public String getItemDescription() {
        return itemDescription;
    }
    public void setItemDescription(String newValue) {
        String old = itemDescription;
        fireBeforePropertyChange(P_ItemDescription, old, newValue);
        this.itemDescription = newValue;
        firePropertyChange(P_ItemDescription, old, this.itemDescription);
    }
    
     
    @OAObjCallback(enabledProperty = SalesOrderItem.P_CustomItem)
    public void itemDescriptionCallback(OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(lowerName = "quantity", displayLength = 6, uiColumnName = "Qty")
    @OAColumn(name = "Quantity", sqlType = java.sql.Types.INTEGER)
    public int getQuantity() {
        return quantity;
    }
    public void setQuantity(int newValue) {
        int old = quantity;
        fireBeforePropertyChange(P_Quantity, old, newValue);
        this.quantity = newValue;
        firePropertyChange(P_Quantity, old, this.quantity);
    }

    @OAProperty(lowerName = "unitPrice", displayName = "Our Item Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, uiColumnLength = 8, uiColumnName = "Item $")
    @OAColumn(name = "UnitPrice", sqlType = java.sql.Types.NUMERIC)
    public double getUnitPrice() {
        return unitPrice;
    }
    public void setUnitPrice(double newValue) {
        double old = unitPrice;
        fireBeforePropertyChange(P_UnitPrice, old, newValue);
        this.unitPrice = newValue;
        firePropertyChange(P_UnitPrice, old, this.unitPrice);
    }
     
    @OAObjCallback(enabledProperty = SalesOrderItem.P_CustomItem, contextVisibleProperty = AppUser.P_User+"."+User.P_CalcPricingReadAccess)
    public void unitPriceCallback(OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(lowerName = "unitWeight", displayName = "Item Weight", displayLength = 6, uiColumnLength = 7, uiColumnName = "Item Wt", hasCustomCode = true)
    @OAColumn(name = "UnitWeight", sqlType = java.sql.Types.INTEGER)
    public int getUnitWeight() {
        return unitWeight;
    }
    public void setUnitWeight(int newValue) {
        int old = unitWeight;
        fireBeforePropertyChange(P_UnitWeight, old, newValue);
        this.unitWeight = newValue;
        firePropertyChange(P_UnitWeight, old, this.unitWeight);
    }
     
    @OAObjCallback(enabledProperty = SalesOrderItem.P_CustomItem)
    public void unitWeightCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            // todo: custom code here;
            callback.ack();
            JLabel lbl = callback.getLabel();
            // use OAJfcUtil static methods ...  lbl.setX(..) / lbl.useX(..)
            if (getQuantity() == 0) lbl.setText("");
            break;
        }
    }

    @OAProperty(lowerName = "servicePrice", displayName = "Service Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, uiColumnName = "Service $")
    @OAColumn(name = "ServicePrice", sqlType = java.sql.Types.NUMERIC)
    public double getServicePrice() {
        return servicePrice;
    }
    public void setServicePrice(double newValue) {
        double old = servicePrice;
        fireBeforePropertyChange(P_ServicePrice, old, newValue);
        this.servicePrice = newValue;
        firePropertyChange(P_ServicePrice, old, this.servicePrice);
    }

    @OAProperty(lowerName = "addOnPrice", displayName = "Add-On Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, uiColumnLength = 8, uiColumnName = "Add-On $", isProcessed = true)
    @OAColumn(name = "AddOnPrice", sqlType = java.sql.Types.NUMERIC)
    public double getAddOnPrice() {
        return addOnPrice;
    }
    public void setAddOnPrice(double newValue) {
        double old = addOnPrice;
        fireBeforePropertyChange(P_AddOnPrice, old, newValue);
        this.addOnPrice = newValue;
        firePropertyChange(P_AddOnPrice, old, this.addOnPrice);
    }
     
    @OAObjCallback(enabledProperty = SalesOrderItem.P_ItemAddOns)
    public void addOnPriceCallback(OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }

    @OAProperty(lowerName = "note", displayLength = 20, isHtml = true)
    @OAColumn(name = "Note", sqlType = java.sql.Types.CLOB)
    public String getNote() {
        return note;
    }
    public void setNote(String newValue) {
        String old = note;
        fireBeforePropertyChange(P_Note, old, newValue);
        this.note = newValue;
        firePropertyChange(P_Note, old, this.note);
    }

    @OAProperty(lowerName = "showSubTotal", displayName = "Show Sub Total", displayLength = 5, uiColumnLength = 8, uiColumnName = "Sub Tot?")
    @OAColumn(name = "ShowSubTotal", sqlType = java.sql.Types.BOOLEAN)
    public boolean getShowSubTotal() {
        return showSubTotal;
    }
    public boolean isShowSubTotal() {
        return getShowSubTotal();
    }
    public void setShowSubTotal(boolean newValue) {
        boolean old = showSubTotal;
        fireBeforePropertyChange(P_ShowSubTotal, old, newValue);
        this.showSubTotal = newValue;
        firePropertyChange(P_ShowSubTotal, old, this.showSubTotal);
    }

    @OAProperty(lowerName = "fakeUnitPrice", displayName = "Fake Unit Price", description = "only used to enter the unit price for a custom item", decimalPlaces = 2, isCurrency = true, trackPrimitiveNull = false, displayLength = 9, uiColumnLength = 15, hasCustomCode = true)
    public double getFakeUnitPrice() {
        if (customItem) return unitPrice;
        AppUser au = ModelDelegate.getLocalAppUser();
        boolean b = false;
        if (au != null)  {
            User user = au.getUser();
            if (user != null) b = user.getPricingAccess() != User.PRICINGACCESS_no;
        }
        if (!b) return 0.00;
        return unitPrice;
    }
    public void setFakeUnitPrice(double newValue) {
        if (!customItem) return;
        setUnitPrice(newValue);
    }
     
    @OAObjCallback(enabledProperty = SalesOrderItem.P_CustomItem)
    public void fakeUnitPriceCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            // todo: custom code here;
            callback.ack();
            JLabel lbl = callback.getLabel();
            // use OAJfcUtil static methods ...  lbl.setX(..) / lbl.useX(..)
    
            AppUser au = ModelDelegate.getLocalAppUser();
            boolean b = false;
            if (au != null)  {
                User user = au.getUser();
                if (user != null) b = user.getPricingAccess() != User.PRICINGACCESS_no;
            }
            if (!customItem && !b) lbl.setText("--");
    
            break;
        }
    }
    @OACalculatedProperty(displayName = "Sell Rate", decimalPlaces = 2, displayLength = 7, columnLength = 9, properties = {P_SalesOrder+"."+SalesOrder.P_PriceCode+"."+PriceCode.P_Rate})
    public double getSellRate() {
        SalesOrder ord = getSalesOrder();
        if (ord == null) return 0.0d;
        PriceCode pc = ord.getPriceCode();
        if (pc == null) return 0.0d;
        double d = pc.getRate();
        return d; 
    }
    public double getPriceCodeRate() {
        return getSellRate();
    }
    @OACalculatedProperty(displayName = "Total Our Item Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, columnLength = 14, columnName = "Tot Item Our $", properties = {P_UnitPrice, P_Quantity})
    public double getTotalItemOurPrice() {
        double d = getUnitPrice() * getQuantity();
        d = OAConv.round(d, 2);
        return d;
    }
    @OAObjCallback(contextVisibleProperty = AppUser.P_User+"."+User.P_CalcPricingReadAccess)
    public void totalItemOurPriceCallback(OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        }
    }
    @OACalculatedProperty(displayName = "Item Sell Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, columnLength = 11, columnName = "Item Sell $", properties = {P_UnitPrice, P_SellRate, P_AddOnPrice})
    public double getItemSellPrice() {
        if (getItem() == null && !getCustomItem()) return 0.0;
        double bdUnit =  getUnitPrice();
        double bdRate = getSellRate();
        bdRate += 100.0d;
        bdRate /= 100.0d;
        double bd = bdUnit * bdRate;
        bd = OAConv.round(bd, 2);
        bd += getAddOnPrice();
        bd = OAConv.round(bd, 2);
        return bd;
    }
    @OAObjCallback
    public void itemSellPriceCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            callback.ack();
            JLabel lbl = callback.getLabel();
            if (getQuantity() == 0) lbl.setText("");
            break;
        }
    }
    @OACalculatedProperty(displayName = "Total Item Sell Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, columnLength = 15, columnName = "Tot Item Sell $", properties = {P_Quantity, P_ItemSellPrice})
    public double getTotalItemSellPrice() {
        if (getItem() == null && !getCustomItem()) return 0.0;
        
        double d = getItemSellPrice();
        d = OAConv.round(d, 2);
    
        int qty = getQuantity();
        if (qty < 1) return 0.0;
        d = d * qty;
        d = OAConv.round(d, 2);
        return d;
    }
    @OAObjCallback
    public void totalItemSellPriceCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            // todo: custom code here;
            callback.ack();
            JLabel lbl = callback.getLabel();
            // use OAJfcUtil static methods ...  lbl.setX(..) / lbl.useX(..)
            if (getQuantity() == 0 && getTotalItemSellPrice() == 0.0) lbl.setText("");
            break;
        }
    }
    @OACalculatedProperty(displayName = "Total Sell Price", decimalPlaces = 2, isCurrency = true, displayLength = 9, columnLength = 10, columnName = "Tot Sell $", properties = {P_ServicePrice, P_TotalItemSellPrice})
    public double getTotalSellPrice() {
        double d = getServicePrice();
        d = d + getTotalItemSellPrice();
        d = OAConv.round(d, 2);
        return d;
    }
    @OAObjCallback
    public void totalSellPriceCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            callback.ack();
            JLabel lbl = callback.getLabel();
            // use OAJfcUtil static methods ...  lbl.setX(..) / lbl.useX(..)
             if (getQuantity() == 0 && getTotalSellPrice() == 0.0) lbl.setText("");
            break;
        }
    }
    @OACalculatedProperty(displayName = "Total Weight", displayLength = 7, columnLength = 6, columnName = "Tot Wt", properties = {P_Quantity, P_UnitWeight})
    public int getTotalWeight() {
        if (getItem() == null && !getCustomItem()) return 0;
        int qty = getQuantity();
        if (qty < 1) return 0;
        qty = getUnitWeight() * qty;
        return qty;
    }
    @OAObjCallback
    public void totalWeightCallback(final OAObjectCallback callback) {
        if (callback == null) return;
        switch (callback.getType()) {
        case RenderLabel:
            // todo: custom code here;
            callback.ack();
            JLabel lbl = callback.getLabel();
            // use OAJfcUtil static methods ...  lbl.setX(..) / lbl.useX(..)
            if (getQuantity() == 0) lbl.setText("");
            break;
        }
    }
    @OACalculatedProperty(displayName = "Allows Item Add On", displayLength = 5, columnLength = 18, properties = {P_CustomItem, P_Item})
    public boolean getAllowsItemAddOn() {
        return getCustomItem() || getItem() != null;
    }

    @OAOne(
        reverseName = Item.P_SalesOrderItems, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_ItemId, toProperty = Item.P_Id)}
    )
    public Item getItem() {
        if (item == null) {
            item = (Item) getObject(P_Item);
        }
        return item;
    }
    public void setItem(Item newValue) {
        fireBeforePropertyChange(P_Item, this.item, newValue);
        Item old = this.item;
        this.item = newValue;
        firePropertyChange(P_Item, old, this.item);
        onItemChanged();
    }
    protected void onItemChanged() {
        if (!beginServerOnly()) return;
        try {
            final Item item = getItem();
            if (item != null) {
                this.setItemDescription(item.getDescription());
                this.setUnitPrice(item.getPrice());
                this.setUnitWeight(item.getWeight());
            }
            else {
                this.setItemDescription(null);
                this.setUnitPrice(0d);
                this.setNull(P_UnitPrice);
                this.setUnitWeight(0);
                this.setNull(P_UnitWeight);
                if (!getCustomItem()) getItemAddOns().removeAll();
            }
        }
        finally {
            endServerOnly();
        }
    }
    @OAColumn(name = "ItemId")
    public int getItemId() {
        return (int) getFkeyProperty(P_ItemId);
    }
    public void setItemId(int newValue) {
        this.item = null;
        setFkeyProperty(P_ItemId, newValue);
        onItemChanged();
    }
    @OAObjCallback(enabledProperty = SalesOrderItem.P_CustomItem, enabledValue = false)
    public void itemCallback(OAObjectCallback em) {
        // CUSTOM
        switch (em.getType()) {
        case SetConfirmForPropertyChange:
            if (getItem() != null) {
                em.setConfirmMessage("ok to replace existing item?");
            }
            break;
        }
    }

    @OAMany(
        displayName = "Item Add Ons", 
        toClass = ItemAddOn.class, 
        reverseName = ItemAddOn.P_SalesOrderItems
    )
    @OALinkTable(name = "ItemAddOnSalesOrderItem", indexName = "ItemAddOnSalesOrderItem", columns = {"SalesOrderItemId"})
    public Hub<ItemAddOn> getItemAddOns() {
        if (hubItemAddOns != null) return hubItemAddOns;
        hubItemAddOns = (Hub<ItemAddOn>) getHub(P_ItemAddOns);
        if (isServer()) { 
            hubItemAddOns.addHubListener(new HubListenerAdapter<ItemAddOn>() {
                @Override
                public void afterAdd(HubEvent<ItemAddOn> e) {
                    beginServerOnly();
                    double d = getAddOnPrice();
                    ItemAddOn ao = e.getObject();
                    double dx = ao.getPrice();
                    setAddOnPrice(d + dx);
                    endServerOnly();
                }
                @Override
                public void afterInsert(HubEvent<ItemAddOn> e) {
                    afterAdd(e);
                }
                @Override
                public void afterRemove(HubEvent<ItemAddOn> e) {
                    beginServerOnly();
                    double d = getAddOnPrice();
                    ItemAddOn ao = e.getObject();
                    double dx = ao.getPrice();
                    setAddOnPrice(Math.max(0.0, d - dx));
                    endServerOnly();
                }
                @Override
                public void afterRemoveAll(HubEvent<ItemAddOn> e) {
                    beginServerOnly();
                    setAddOnPrice(0.0d);
                    endServerOnly();
                }
            });
        }
        return hubItemAddOns;
    }
    @OAObjCallback(enabledProperty = SalesOrderItem.P_AllowsItemAddOn)
    public void itemAddOnsCallback(OAObjectCallback cb) {
        if (cb == null) return;
        switch (cb.getType()) {
        }
    }

    @OAOne(
        displayName = "Sales Order", 
        reverseName = SalesOrder.P_SalesOrderItems, 
        required = true, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_SalesOrderId, toProperty = SalesOrder.P_Id)}
    )
    public SalesOrder getSalesOrder() {
        if (salesOrder == null) {
            salesOrder = (SalesOrder) getObject(P_SalesOrder);
        }
        return salesOrder;
    }
    public void setSalesOrder(SalesOrder newValue) {
        SalesOrder old = this.salesOrder;
        fireBeforePropertyChange(P_SalesOrder, old, newValue);
        this.salesOrder = newValue;
        firePropertyChange(P_SalesOrder, old, this.salesOrder);
    }
    @OAProperty(isFkeyOnly = true)
    @OAColumn(name = "SalesOrderId")
    public Integer getSalesOrderId() {
        return (Integer) getFkeyProperty(P_SalesOrderId);
    }
    public void setSalesOrderId(Integer newValue) {
        this.salesOrder = null;
        setFkeyProperty(P_SalesOrderId, newValue);
    }

    @OAOne(
        displayName = "Service Code", 
        reverseName = ServiceCode.P_SalesOrderItems, 
        allowCreateNew = false, 
        fkeys = {@OAFkey(fromProperty = P_ServiceCodeId, toProperty = ServiceCode.P_Id)}
    )
    public ServiceCode getServiceCode() {
        if (serviceCode == null) {
            serviceCode = (ServiceCode) getObject(P_ServiceCode);
        }
        return serviceCode;
    }
    public void setServiceCode(ServiceCode newValue) {
        fireBeforePropertyChange(P_ServiceCode, this.serviceCode, newValue);
        ServiceCode old = this.serviceCode;
        this.serviceCode = newValue;
        firePropertyChange(P_ServiceCode, old, this.serviceCode);
        onServiceCodeChanged();
    }
    protected void onServiceCodeChanged() {
        if (!beginServerOnly()) return;
        ServiceCode sc = getServiceCode();
        try {
            if (sc == null) setServicePrice(0d);
            else {
                double d = sc.getPrice();
                setServicePrice(d);
            }
        }
        finally {
            endServerOnly();
        }
    }
    @OAColumn(name = "ServiceCodeId")
    public int getServiceCodeId() {
        return (int) getFkeyProperty(P_ServiceCodeId);
    }
    public void setServiceCodeId(int newValue) {
        this.serviceCode = null;
        setFkeyProperty(P_ServiceCodeId, newValue);
        onServiceCodeChanged();
    }
    @OAObjCallback
    public void serviceCodeCallback(OAObjectCallback em) {
        switch (em.getType()) {
        case SetConfirmForPropertyChange:
            double d = getServicePrice();
            if (d > 0d) {
                em.setConfirmMessage("ok to change the service price?");
            }
            break;
        }
    }
    @OAMethod(displayName = "Assign Item Add On")
    public void assignSameItemAddOn(SalesOrderItemAssignSameItemAddOnMethod data) {
        if (data == null) return;
        ItemAddOn ao = data.getItemAddOn();
        getItemAddOns().add(ao);
    }
    public static void assignSameItemAddOn(Hub<SalesOrderItem> hub, SalesOrderItemAssignSameItemAddOnMethod data) {
        for (SalesOrderItem salesOrderItem : hub) {
            salesOrderItem.assignSameItemAddOn(data);
        }
    }
    @OAObjCallback(enabledProperty = SalesOrderItem.P_AllowsItemAddOn)
    public void assignSameItemAddOnCallback(OAObjectCallback cb) {
    }

    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        this.seq = rs.getInt(2);
        this.location = rs.getString(3);
        this.customItem = (rs.getShort(4) == 1);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_CustomItem, rs.wasNull());
        this.itemDescription = rs.getString(5);
        this.quantity = rs.getInt(6);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_Quantity, rs.wasNull());
        this.unitPrice = rs.getDouble(7);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_UnitPrice, rs.wasNull());
        this.unitWeight = rs.getInt(8);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_UnitWeight, rs.wasNull());
        this.servicePrice = rs.getDouble(9);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_ServicePrice, rs.wasNull());
        this.addOnPrice = rs.getDouble(10);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_AddOnPrice, rs.wasNull());
        this.note = rs.getString(11);
        this.showSubTotal = (rs.getShort(12) == 1);
        OAObjectInfoDelegate.setPrimitiveNull(this, P_ShowSubTotal, rs.wasNull());
        int itemFkey = rs.getInt(13);
        setFkeyProperty(P_Item, rs.wasNull() ? null : itemFkey);
        int salesOrderFkey = rs.getInt(14);
        setFkeyProperty(P_SalesOrder, rs.wasNull() ? null : salesOrderFkey);
        int serviceCodeFkey = rs.getInt(15);
        setFkeyProperty(P_ServiceCode, rs.wasNull() ? null : serviceCodeFkey);

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
