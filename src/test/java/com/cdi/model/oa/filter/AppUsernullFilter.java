// Generated by OABuilder
package com.cdi.model.oa.filter;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import java.util.*;
import com.cdi.model.search.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "null", displayName = "", hasInputParams = false)
public class AppUsernullFilter extends OAObject implements CustomHubFilter<AppUser> {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(AppUsernullFilter.class.getName());

    public static final String PPCode = ":null()";
    private Hub<AppUser> hubMaster;
    private Hub<AppUser> hub;
    private HubFilter<AppUser> hubFilter;
    private OAObjectCacheFilter<AppUser> cacheFilter;
    private boolean bUseObjectCache;

    public AppUsernullFilter(Hub<AppUser> hub) {
        this(null, hub, true);
    }
    public AppUsernullFilter(Hub<AppUser> hubMaster, Hub<AppUser> hub) {
        this(hubMaster, hub, false);
    }
    public AppUsernullFilter(Hub<AppUser> hubMaster, Hub<AppUser> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }


    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<AppUser> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<AppUser>(hubMaster, hub) {
            @Override
            public boolean isUsed(AppUser appUser) {
                return AppUsernullFilter.this.isUsed(appUser);
            }
        };
        hubFilter.addDependentProperty(AppUserPP.fullName(), false);
        hubFilter.addDependentProperty(AppUserPP.firstName(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<AppUser> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        cacheFilter = new OAObjectCacheFilter<AppUser>(hub) {
            @Override
            public boolean isUsed(AppUser appUser) {
                return AppUsernullFilter.this.isUsed(appUser);
            }
            @Override
            protected void reselect() {
                AppUsernullFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(AppUserPP.fullName(), false);
        cacheFilter.addDependentProperty(AppUserPP.firstName(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
   public boolean isUsed(AppUser appUser) {
        // custom code here needs to be put in OABuilder
        return true;
   }
}
