// Generated by OABuilder
package com.cdi.model.oa.filter;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import java.util.*;
import com.cdi.model.search.*;
import com.cdi.model.oa.search.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "Active", displayName = "Active", hasInputParams = false)
public class CustomerActiveFilter extends OAObject implements CustomHubFilter<Customer> {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(CustomerActiveFilter.class.getName());

    public static final String PPCode = ":Active()";
    private Hub<Customer> hubMaster;
    private Hub<Customer> hub;
    private HubFilter<Customer> hubFilter;
    private OAObjectCacheFilter<Customer> cacheFilter;
    private boolean bUseObjectCache;

    public CustomerActiveFilter() {
        this(null, null, false);
    }
    public CustomerActiveFilter(Hub<Customer> hub) {
        this(null, hub, true);
    }
    public CustomerActiveFilter(Hub<Customer> hubMaster, Hub<Customer> hub) {
        this(hubMaster, hub, false);
    }
    public CustomerActiveFilter(Hub<Customer> hubMaster, Hub<Customer> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }


    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<Customer> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<Customer>(hubMaster, hub) {
            @Override
            public boolean isUsed(Customer customer) {
                return CustomerActiveFilter.this.isUsed(customer);
            }
        };
        hubFilter.addDependentProperty(CustomerPP.inactiveDate(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<Customer> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        cacheFilter = new OAObjectCacheFilter<Customer>(hub) {
            @Override
            public boolean isUsed(Customer customer) {
                return CustomerActiveFilter.this.isUsed(customer);
            }
            @Override
            protected void reselect() {
                CustomerActiveFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(CustomerPP.inactiveDate(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(Customer customer) {
        OADate d = customer.getInactiveDate();
        return d == null;
    }
}
