// Generated by OABuilder
package com.cdi.model.oa.filter;

import java.util.*;
import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.cache.OAObjectCacheFilter;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.hub.filter.CustomHubFilter;
import com.viaoa.hub.filter.HubFilter;
import com.viaoa.util.*;
import java.util.*;
import com.cdi.model.search.*;
import com.cdi.model.oa.search.*;
import com.viaoa.func.OAFunction;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "HasOpenOrderItems", displayName = "Has Open Order Items", hasInputParams = false)
public class OpenOrderItemHasOpenOrderItemsFilter extends OAObject implements CustomHubFilter<OpenOrderItem> {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(OpenOrderItemHasOpenOrderItemsFilter.class.getName());


    public static final String PPCode = ":HasOpenOrderItems()";
    private Hub<OpenOrderItem> hubMaster;
    private Hub<OpenOrderItem> hub;
    private HubFilter<OpenOrderItem> hubFilter;
    private OAObjectCacheFilter<OpenOrderItem> cacheFilter;
    private boolean bUseObjectCache;

    public OpenOrderItemHasOpenOrderItemsFilter() {
        this(null, null, false);
    }
    public OpenOrderItemHasOpenOrderItemsFilter(Hub<OpenOrderItem> hub) {
        this(null, hub, true);
    }
    public OpenOrderItemHasOpenOrderItemsFilter(Hub<OpenOrderItem> hubMaster, Hub<OpenOrderItem> hub) {
        this(hubMaster, hub, false);
    }
    public OpenOrderItemHasOpenOrderItemsFilter(Hub<OpenOrderItem> hubMaster, Hub<OpenOrderItem> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<OpenOrderItem> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<OpenOrderItem>(hubMaster, hub) {
            @Override
            public boolean isUsed(OpenOrderItem openOrderItem) {
                return OpenOrderItemHasOpenOrderItemsFilter.this.isUsed(openOrderItem);
            }
        };
        hubFilter.addDependentProperty(OpenOrderItemPP.orderCount(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<OpenOrderItem> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        hub.onBeforeRefresh(e -> reselect());
        cacheFilter = new OAObjectCacheFilter<OpenOrderItem>(hub) {
            @Override
            public boolean isUsed(OpenOrderItem openOrderItem) {
                return OpenOrderItemHasOpenOrderItemsFilter.this.isUsed(openOrderItem);
            }
            @Override
            protected void reselect() {
                OpenOrderItemHasOpenOrderItemsFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(OpenOrderItemPP.orderCount(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(OpenOrderItem openOrderItem) {
        int x = openOrderItem == null ? 0 : openOrderItem.getOrderCount();
        return x > 0;
    }
}
