// Generated by OABuilder
package com.cdi.model.oa.filter;

import java.util.*;
import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.cache.OAObjectCacheFilter;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.hub.filter.CustomHubFilter;
import com.viaoa.hub.filter.HubFilter;
import com.viaoa.util.*;
import java.util.*;
import com.cdi.model.search.*;
import com.cdi.model.oa.search.*;


@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "HasRemainingQuantity", displayName = "Has Remaining Quantity", hasInputParams = false)
public class OrderItemHasRemainingQuantityFilter extends OAObject implements CustomHubFilter<OrderItem> {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(OrderItemHasRemainingQuantityFilter.class.getName());


    public static final String PPCode = ":HasRemainingQuantity()";
    private Hub<OrderItem> hubMaster;
    private Hub<OrderItem> hub;
    private HubFilter<OrderItem> hubFilter;
    private OAObjectCacheFilter<OrderItem> cacheFilter;
    private boolean bUseObjectCache;

    public OrderItemHasRemainingQuantityFilter() {
        this(null, null, false);
    }
    public OrderItemHasRemainingQuantityFilter(Hub<OrderItem> hub) {
        this(null, hub, true);
    }
    public OrderItemHasRemainingQuantityFilter(Hub<OrderItem> hubMaster, Hub<OrderItem> hub) {
        this(hubMaster, hub, false);
    }
    public OrderItemHasRemainingQuantityFilter(Hub<OrderItem> hubMaster, Hub<OrderItem> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<OrderItem> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<OrderItem>(hubMaster, hub) {
            @Override
            public boolean isUsed(OrderItem orderItem) {
                return OrderItemHasRemainingQuantityFilter.this.isUsed(orderItem);
            }
        };
        hubFilter.addDependentProperty(OrderItemPP.remainingQuantity(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<OrderItem> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        hub.onBeforeRefresh(e -> reselect());
        cacheFilter = new OAObjectCacheFilter<OrderItem>(hub) {
            @Override
            public boolean isUsed(OrderItem orderItem) {
                return OrderItemHasRemainingQuantityFilter.this.isUsed(orderItem);
            }
            @Override
            protected void reselect() {
                OrderItemHasRemainingQuantityFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(OrderItemPP.remainingQuantity(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(OrderItem orderItem) {
        int x = orderItem.getRemainingQuantity();
        return x > 0;
    }
}
