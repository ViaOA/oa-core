// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.ds.*;
import com.viaoa.util.filter.OAQueryFilter;

@OAClass(useDataSource=false, localOnly=true)
public class ContactSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(ContactSearch.class.getName());
    public static final String P_Id = "Id";
    public static final String P_Name = "Name";
    public static final String P_NameUseNull = "NameUseNull";
    public static final String P_NameUseNotNull = "NameUseNotNull";
    public static final String P_ContactCustomer = "ContactCustomer";
    public static final String P_UseContactCustomerSearch = "UseContactCustomerSearch";
    public static final String P_ContactPhones = "ContactPhones";
    public static final String P_UseContactPhonesSearch = "UseContactPhonesSearch";
    public static final String P_MaxResults = "MaxResults";
    public static final String P_SortByType = "SortByType";
    public static final String P_SortByDesc = "SortByDesc";

    protected int id;
    protected String name;
    protected boolean nameUseNull;
    protected boolean nameUseNotNull;
    protected Customer contactCustomer;
    protected boolean useContactCustomerSearch;
    protected CustomerSearch searchContactCustomer;
    protected Phone contactPhones;
    protected boolean useContactPhonesSearch;
    protected PhoneSearch searchContactPhones;
    protected int maxResults;
    protected int sortByType;
    protected boolean sortByDesc;

    public static final int SORTBYTYPE_None = 0;
    public static final int SORTBYTYPE_name = 1;
    public static final int SORTBYTYPE_email = 2;
    public static final int SORTBYTYPE_name1 = 3;
    public static final Hub<String> hubSortByType;
    static {
        hubSortByType = new Hub<String>(String.class);
        hubSortByType.add("None");
        hubSortByType.add("Name");
        hubSortByType.add("Email");
        hubSortByType.add("Name1");
    }

    public ContactSearch() {
        reset();
    }

    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
      

    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      
    public boolean getNameUseNull() {
        return nameUseNull;
    }
    public void setNameUseNull(boolean newValue) {
        boolean old = this.nameUseNull;
        this.nameUseNull = newValue;
        firePropertyChange(P_NameUseNull, old, this.nameUseNull);
    }
    public boolean getNameUseNotNull() {
        return nameUseNotNull;
    }
    public void setNameUseNotNull(boolean newValue) {
        boolean old = this.nameUseNotNull;
        this.nameUseNotNull = newValue;
        firePropertyChange(P_NameUseNotNull, old, this.nameUseNotNull);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public int getSortByType() {
        return this.sortByType;
    }
    public void setSortByType(int newValue) {
        fireBeforePropertyChange(P_SortByType, this.sortByType, newValue);
        int old = this.sortByType;
        this.sortByType = newValue;
        firePropertyChange(P_SortByType, old, this.sortByType);
    }

    public boolean getSortByDesc() {
        return this.sortByDesc;
    }
    public void setSortByDesc(boolean newValue) {
        fireBeforePropertyChange(P_SortByDesc, this.sortByDesc, newValue);
        boolean old = this.sortByDesc;
        this.sortByDesc = newValue;
        firePropertyChange(P_SortByDesc, old, this.sortByDesc);
    }

    @OAOne
    public Customer getContactCustomer() {
        if (contactCustomer == null) {
            contactCustomer = (Customer) getObject(P_ContactCustomer);
        }
        return contactCustomer;
    }
    public void setContactCustomer(Customer newValue) {
        Customer old = this.contactCustomer;
        this.contactCustomer = newValue;
        firePropertyChange(P_ContactCustomer, old, this.contactCustomer);
    }
    public boolean getUseContactCustomerSearch() {
        return useContactCustomerSearch;
    }
    public void setUseContactCustomerSearch(boolean newValue) {
        boolean old = this.useContactCustomerSearch;
        this.useContactCustomerSearch = newValue;
        firePropertyChange(P_UseContactCustomerSearch, old, this.useContactCustomerSearch);
    }
    public CustomerSearch getContactCustomerSearch() {
        return this.searchContactCustomer;
    }
    public void setContactCustomerSearch(CustomerSearch newValue) {
        this.searchContactCustomer = newValue;
    }

    @OAOne
    public Phone getContactPhones() {
        if (contactPhones == null) {
            contactPhones = (Phone) getObject(P_ContactPhones);
        }
        return contactPhones;
    }
    public void setContactPhones(Phone newValue) {
        Phone old = this.contactPhones;
        this.contactPhones = newValue;
        firePropertyChange(P_ContactPhones, old, this.contactPhones);
    }
    public boolean getUseContactPhonesSearch() {
        return useContactPhonesSearch;
    }
    public void setUseContactPhonesSearch(boolean newValue) {
        boolean old = this.useContactPhonesSearch;
        this.useContactPhonesSearch = newValue;
        firePropertyChange(P_UseContactPhonesSearch, old, this.useContactPhonesSearch);
    }
    public PhoneSearch getContactPhonesSearch() {
        return this.searchContactPhones;
    }
    public void setContactPhonesSearch(PhoneSearch newValue) {
        this.searchContactPhones = newValue;
    }

    public void reset() {
        setId(0);
        setNull(P_Id);
        setName(null);
        setNameUseNull(false);
        setNameUseNotNull(false);
        setContactCustomer(null);
        setUseContactCustomerSearch(false);
        setContactPhones(null);
        setUseContactPhonesSearch(false);
        setSortByType(SORTBYTYPE_None);
        setNull(P_SortByType);
        setSortByDesc(false);
        setNull(P_SortByDesc);
    }

    public boolean isDataEntered() {
        if (!isNull(P_Id)) return true;
        if (getName() != null) return true;
        if (getNameUseNull()) return true;if (getNameUseNotNull()) return true;
        if (getNameUseNull()) return true;
        if (getNameUseNotNull()) return true;
        if (getContactCustomer() != null) return true;
        if (getUseContactCustomerSearch()) return true;
        if (getContactPhones() != null) return true;
        if (getUseContactPhonesSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<Contact> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<Contact> f = new OAQueryFilter<Contact>(Contact.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<Contact> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<Contact> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<Contact> getSelect() {
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];

        switch (getSortByType()) {
            case SORTBYTYPE_None: break;
            case SORTBYTYPE_name: sortOrder = Contact.P_Name; break;
            case SORTBYTYPE_email: sortOrder = Contact.P_Email; break;
            case SORTBYTYPE_name1: sortOrder = ContactPP.customer().name(); break;
        }
        if (OAString.isNotEmpty(sortOrder) && getSortByDesc()) sortOrder += " DESC";

        OAFinder finder = null;
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += Contact.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + Contact.P_Name + " = null OR " + Contact.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += Contact.P_Name + " != null";
        }
        else if (!OAString.isEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.name.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += Contact.P_Name + " LIKE ?";
            }
            else {
                sql += Contact.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!useContactCustomerSearch && getContactCustomer() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ContactPP.customer().pp + " = ?";
            args = OAArray.add(Object.class, args, getContactCustomer());
            finder = new OAFinder<Customer, Contact>(getContactCustomer(), Customer.P_Contacts);
        }
        if (!useContactPhonesSearch && getContactPhones() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ContactPP.phones().pp + " = ?";
            args = OAArray.add(Object.class, args, getContactPhones());
            finder = new OAFinder<Phone, Contact>(getContactPhones(), Phone.P_Contact);
        }

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<Contact> select = new OASelect<Contact>(Contact.class, sql, args, sortOrder);
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFilter(this.getCustomFilter());
        select.setFinder(finder);
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useContactCustomerSearch && getContactCustomerSearch() != null) {
            getContactCustomerSearch().appendSelect(ContactPP.customer().pp, select);
        }
        if (useContactPhonesSearch && getContactPhonesSearch() != null) {
            getContactPhonesSearch().appendSelect(ContactPP.phones().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += Contact.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + Contact.P_Name + " = null OR " + Contact.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += Contact.P_Name + " != null";
        }
        else if (!OAString.isEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.name.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += Contact.P_Name + " LIKE ?";
            }
            else {
                sql += Contact.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!useContactCustomerSearch && getContactCustomer() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ContactPP.customer().pp + " = ?";
            args = OAArray.add(Object.class, args, getContactCustomer());
        }
        if (useContactCustomerSearch && getContactCustomerSearch() != null) {
            getContactCustomerSearch().appendSelect(prefix + ContactPP.customer().pp, select);
        }
        if (!useContactPhonesSearch && getContactPhones() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ContactPP.phones().pp + " = ?";
            args = OAArray.add(Object.class, args, getContactPhones());
        }
        if (useContactPhonesSearch && getContactPhonesSearch() != null) {
            getContactPhonesSearch().appendSelect(prefix + ContactPP.phones().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<Contact> filterDataSourceFilter;
    public OAFilter<Contact> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<Contact>() {
            @Override
            public boolean isUsed(Contact contact) {
                return ContactSearch.this.isUsedForDataSourceFilter(contact);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<Contact> filterCustomFilter;
    public OAFilter<Contact> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<Contact>() {
            @Override
            public boolean isUsed(Contact contact) {
                boolean b = ContactSearch.this.isUsedForCustomFilter(contact);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(contact);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(Contact searchContact) {
        if (!isNull(P_Id)) {
            if (!OACompare.isEqual(searchContact.getId(), id)) return false;
        }
        if (nameUseNull) {
            if (OACompare.isNotEmpty(searchContact.getName())) return false;
        }
        else if (nameUseNotNull) {
            if (OACompare.isEmpty(searchContact.getName())) return false;
        }
        else if (name != null) {
            String s = getName();
            if (s != null && s.indexOf('*') < 0 && s.indexOf('%') < 0) s += '*';
            if (!OACompare.isLike(searchContact.getName(), s)) return false;
        }
        if (contactCustomer != null) {
            if (!OACompare.isEqual(searchContact.getCustomer(), contactCustomer)) return false;
        }
        if (contactPhones != null) {
            if (!OACompare.isIn(contactPhones, searchContact.getPhones())) return false;
        }
        if (useContactCustomerSearch && getContactCustomerSearch() != null) {
            Customer customer = (Customer) searchContact.getProperty(ContactPP.customer().pp);
            if (customer == null) return false;
            if (!getContactCustomerSearch().isUsedForDataSourceFilter(customer)) return false;
        }
        if (useContactPhonesSearch && getContactPhonesSearch() != null) {
            Phone phone = (Phone) searchContact.getProperty(ContactPP.phones().pp);
            if (phone == null) return false;
            if (!getContactPhonesSearch().isUsedForDataSourceFilter(phone)) return false;
        }
        return true;
    }
    public boolean isUsedForCustomFilter(Contact searchContact) {
        return true;
    }
}
