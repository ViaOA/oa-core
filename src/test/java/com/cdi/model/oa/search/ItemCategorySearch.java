// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.*;
import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.model.oa.VString;
import com.viaoa.util.*;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ItemCategorySearch extends OAObject {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(ItemCategorySearch.class.getName());

    public static final String P_Id = "Id";
    public static final String P_Name = "Name";
    public static final String P_NameUseNull = "NameUseNull";
    public static final String P_NameUseNotNull = "NameUseNotNull";
    public static final String P_Description = "Description";
    public static final String P_DescriptionUseNull = "DescriptionUseNull";
    public static final String P_DescriptionUseNotNull = "DescriptionUseNotNull";
    public static final String P_CustomQuery = "CustomQuery";
    public static final String P_MaxResults = "MaxResults";
    public static final String P_SortByType = "SortByType";
    public static final String P_SortByDesc = "SortByDesc";

    protected int id;
    protected String name;
    protected boolean nameUseNull;
    protected boolean nameUseNotNull;
    protected String description;
    protected boolean descriptionUseNull;
    protected boolean descriptionUseNotNull;
    protected String customQuery;
    protected int maxResults;
    protected int sortByType;
    protected boolean sortByDesc;

    public static final int SORTBYTYPE_None = 0;
    public static final int SORTBYTYPE_name = 1;

    public ItemCategorySearch() {
        reset();
    }

    @OAProperty(lowerName = "id", displayLength = 6)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
      
    @OAProperty(lowerName = "name", maxLength = 35, displayLength = 20, uiColumnLength = 15)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      
    public boolean getNameUseNull() {
        return nameUseNull;
    }
    public void setNameUseNull(boolean newValue) {
        boolean old = this.nameUseNull;
        this.nameUseNull = newValue;
        firePropertyChange(P_NameUseNull, old, this.nameUseNull);
    }
    public boolean getNameUseNotNull() {
        return nameUseNotNull;
    }
    public void setNameUseNotNull(boolean newValue) {
        boolean old = this.nameUseNotNull;
        this.nameUseNotNull = newValue;
        firePropertyChange(P_NameUseNotNull, old, this.nameUseNotNull);
    }
    @OAProperty(lowerName = "description", maxLength = 150, displayLength = 20)
    public String getDescription() {
        return description;
    }
    public void setDescription(String newValue) {
        String old = description;
        fireBeforePropertyChange(P_Description, old, newValue);
        this.description = newValue;
        firePropertyChange(P_Description, old, this.description);
    }
      
    public boolean getDescriptionUseNull() {
        return descriptionUseNull;
    }
    public void setDescriptionUseNull(boolean newValue) {
        boolean old = this.descriptionUseNull;
        this.descriptionUseNull = newValue;
        firePropertyChange(P_DescriptionUseNull, old, this.descriptionUseNull);
    }
    public boolean getDescriptionUseNotNull() {
        return descriptionUseNotNull;
    }
    public void setDescriptionUseNotNull(boolean newValue) {
        boolean old = this.descriptionUseNotNull;
        this.descriptionUseNotNull = newValue;
        firePropertyChange(P_DescriptionUseNotNull, old, this.descriptionUseNotNull);
    }

    public String getCustomQuery() {
        return customQuery;
    }
    public void setCustomQuery(String newValue) {
        fireBeforePropertyChange(P_CustomQuery, this.customQuery, newValue);
        String old = customQuery;
        this.customQuery = newValue;
        firePropertyChange(P_CustomQuery, old, this.customQuery);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public int getSortByType() {
        return this.sortByType;
    }
    public void setSortByType(int newValue) {
        fireBeforePropertyChange(P_SortByType, this.sortByType, newValue);
        int old = this.sortByType;
        this.sortByType = newValue;
        firePropertyChange(P_SortByType, old, this.sortByType);
    }

    public boolean getSortByDesc() {
        return this.sortByDesc;
    }
    public void setSortByDesc(boolean newValue) {
        fireBeforePropertyChange(P_SortByDesc, this.sortByDesc, newValue);
        boolean old = this.sortByDesc;
        this.sortByDesc = newValue;
        firePropertyChange(P_SortByDesc, old, this.sortByDesc);
    }

    public void reset() {
        setId(0);
        setNull(P_Id);
        setName(null);
        setNameUseNull(false);
        setNameUseNotNull(false);
        setDescription(null);
        setDescriptionUseNull(false);
        setDescriptionUseNotNull(false);
        setCustomQuery(null);
        setSortByType(SORTBYTYPE_None);
        setNull(P_SortByType);
        setSortByDesc(false);
        setNull(P_SortByDesc);
    }

    public boolean isDataEntered() {
        if (!isNull(P_Id)) return true;
        if (getName() != null) return true;
        if (getNameUseNull()) return true;
        if (getNameUseNotNull()) return true;
        if (getDescription() != null) return true;
        if (getDescriptionUseNull()) return true;
        if (getDescriptionUseNotNull()) return true;
        if (getCustomQuery() != null) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ItemCategory> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ItemCategory> f = new OAQueryFilter<ItemCategory>(ItemCategory.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ItemCategory> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ItemCategory> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ItemCategory> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];

        switch (getSortByType()) {
            case SORTBYTYPE_None: break;
            case SORTBYTYPE_name: sortOrder = ItemCategory.P_Name; break;
        }
        if (OAString.isNotEmpty(sortOrder) && getSortByDesc()) sortOrder += " DESC";

        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ItemCategory.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + ItemCategory.P_Name + " = null OR " + ItemCategory.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ItemCategory.P_Name + " != null";
        }
        else if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += ItemCategory.P_Name + " LIKE ?";
            }
            else {
                sql += ItemCategory.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (descriptionUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + ItemCategory.P_Description + " = null OR " + ItemCategory.P_Description + " == '')";
        }
        else if (descriptionUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ItemCategory.P_Description + " != null";
        }
        else if (OAString.isNotEmpty(this.description)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(description);
            if (val.indexOf("%") >= 0) {
                sql += ItemCategory.P_Description + " LIKE ?";
            }
            else {
                sql += ItemCategory.P_Description + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
    if (OAString.isNotEmpty(this.customQuery)) {
        if (sql.length() > 0) sql += " AND ";
        sql += "(" + getCustomQuery() + ")";
    }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ItemCategory> select = new OASelect<ItemCategory>(ItemCategory.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ItemCategory.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + prefix + ItemCategory.P_Name + " = null OR " + prefix + ItemCategory.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ItemCategory.P_Name + " != null";
        }
        else if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ItemCategory.P_Name + " LIKE ?";
            }
            else {
                sql += prefix + ItemCategory.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (descriptionUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + prefix + ItemCategory.P_Description + " = null OR " + prefix + ItemCategory.P_Description + " == '')";
        }
        else if (descriptionUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ItemCategory.P_Description + " != null";
        }
        else if (OAString.isNotEmpty(this.description)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(description);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ItemCategory.P_Description + " LIKE ?";
            }
            else {
                sql += prefix + ItemCategory.P_Description + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
    if (OAString.isNotEmpty(this.customQuery)) {
        if (sql.length() > 0) sql += " AND ";
        sql += "(" + getCustomQuery() + ")";
    }
        select.add(sql, args);
    }

    private OAFilter<ItemCategory> filterDataSourceFilter;
    public OAFilter<ItemCategory> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ItemCategory>() {
            @Override
            public boolean isUsed(ItemCategory itemCategory) {
                return ItemCategorySearch.this.isUsedForDataSourceFilter(itemCategory);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ItemCategory> filterCustomFilter;
    public OAFilter<ItemCategory> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ItemCategory>() {
            @Override
            public boolean isUsed(ItemCategory itemCategory) {
                boolean b = ItemCategorySearch.this.isUsedForCustomFilter(itemCategory);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ItemCategory searchItemCategory) {
        return true;
    }
    public boolean isUsedForCustomFilter(ItemCategory searchItemCategory) {
        return true;
    }
}
