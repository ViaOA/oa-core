// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.datasource.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.OAQueryFilter;

@OAClass(useDataSource=false, localOnly=true)
public class QuickbookSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(QuickbookSearch.class.getName());
    public static final String P_MaxResults = "MaxResults";

    protected int maxResults;

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<Quickbook> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<Quickbook> f = new OAQueryFilter<Quickbook>(Quickbook.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<Quickbook> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<Quickbook> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<Quickbook> getSelect() {
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<Quickbook> select = new OASelect<Quickbook>(Quickbook.class, sql, args, sortOrder);
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFilter(this.getCustomFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        select.add(sql, args);
    }

    private OAFilter<Quickbook> filterDataSourceFilter;
    public OAFilter<Quickbook> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<Quickbook>() {
            @Override
            public boolean isUsed(Quickbook quickbook) {
                return QuickbookSearch.this.isUsedForDataSourceFilter(quickbook);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<Quickbook> filterCustomFilter;
    public OAFilter<Quickbook> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<Quickbook>() {
            @Override
            public boolean isUsed(Quickbook quickbook) {
                boolean b = QuickbookSearch.this.isUsedForCustomFilter(quickbook);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(quickbook);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(Quickbook searchQuickbook) {
        return true;
    }
    public boolean isUsedForCustomFilter(Quickbook searchQuickbook) {
        return true;
    }
}
