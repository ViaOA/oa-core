// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.*;
import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.model.oa.VString;
import com.viaoa.util.*;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ServiceCodeSearch extends OAObject {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(ServiceCodeSearch.class.getName());

    public static final String P_Id = "Id";
    public static final String P_Name = "Name";
    public static final String P_NameUseNull = "NameUseNull";
    public static final String P_NameUseNotNull = "NameUseNotNull";
    public static final String P_ItemCode = "ItemCode";
    public static final String P_ItemCodeUseNull = "ItemCodeUseNull";
    public static final String P_ItemCodeUseNotNull = "ItemCodeUseNotNull";
    public static final String P_Description = "Description";
    public static final String P_DescriptionUseNull = "DescriptionUseNull";
    public static final String P_DescriptionUseNotNull = "DescriptionUseNotNull";
    public static final String P_Price = "Price";
    public static final String P_Price2 = "Price2";
    public static final String P_CustomQuery = "CustomQuery";
    public static final String P_MaxResults = "MaxResults";
    public static final String P_SortByType = "SortByType";
    public static final String P_SortByDesc = "SortByDesc";

    protected int id;
    protected String name;
    protected boolean nameUseNull;
    protected boolean nameUseNotNull;
    protected String itemCode;
    protected boolean itemCodeUseNull;
    protected boolean itemCodeUseNotNull;
    protected String description;
    protected boolean descriptionUseNull;
    protected boolean descriptionUseNotNull;
    protected double price;
    protected double price2;
    protected String customQuery;
    protected int maxResults;
    protected int sortByType;
    protected boolean sortByDesc;

    public static final int SORTBYTYPE_None = 0;
    public static final int SORTBYTYPE_name = 1;
    public static final int SORTBYTYPE_id = 2;
    public static final int SORTBYTYPE_description = 3;
    public static final int SORTBYTYPE_price = 4;
    public static final int SORTBYTYPE_itemCode = 5;

    public ServiceCodeSearch() {
        reset();
    }

    @OAProperty(lowerName = "id", displayLength = 6)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
      
    @OAProperty(lowerName = "name", maxLength = 35, displayLength = 20)
    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      
    public boolean getNameUseNull() {
        return nameUseNull;
    }
    public void setNameUseNull(boolean newValue) {
        boolean old = this.nameUseNull;
        this.nameUseNull = newValue;
        firePropertyChange(P_NameUseNull, old, this.nameUseNull);
    }
    public boolean getNameUseNotNull() {
        return nameUseNotNull;
    }
    public void setNameUseNotNull(boolean newValue) {
        boolean old = this.nameUseNotNull;
        this.nameUseNotNull = newValue;
        firePropertyChange(P_NameUseNotNull, old, this.nameUseNotNull);
    }
    @OAProperty(lowerName = "itemCode", displayName = "Item Code", maxLength = 20, displayLength = 10)
    public String getItemCode() {
        return itemCode;
    }
    public void setItemCode(String newValue) {
        String old = itemCode;
        fireBeforePropertyChange(P_ItemCode, old, newValue);
        this.itemCode = newValue;
        firePropertyChange(P_ItemCode, old, this.itemCode);
    }
      
    public boolean getItemCodeUseNull() {
        return itemCodeUseNull;
    }
    public void setItemCodeUseNull(boolean newValue) {
        boolean old = this.itemCodeUseNull;
        this.itemCodeUseNull = newValue;
        firePropertyChange(P_ItemCodeUseNull, old, this.itemCodeUseNull);
    }
    public boolean getItemCodeUseNotNull() {
        return itemCodeUseNotNull;
    }
    public void setItemCodeUseNotNull(boolean newValue) {
        boolean old = this.itemCodeUseNotNull;
        this.itemCodeUseNotNull = newValue;
        firePropertyChange(P_ItemCodeUseNotNull, old, this.itemCodeUseNotNull);
    }
    @OAProperty(lowerName = "description", displayLength = 20)
    public String getDescription() {
        return description;
    }
    public void setDescription(String newValue) {
        String old = description;
        fireBeforePropertyChange(P_Description, old, newValue);
        this.description = newValue;
        firePropertyChange(P_Description, old, this.description);
    }
      
    public boolean getDescriptionUseNull() {
        return descriptionUseNull;
    }
    public void setDescriptionUseNull(boolean newValue) {
        boolean old = this.descriptionUseNull;
        this.descriptionUseNull = newValue;
        firePropertyChange(P_DescriptionUseNull, old, this.descriptionUseNull);
    }
    public boolean getDescriptionUseNotNull() {
        return descriptionUseNotNull;
    }
    public void setDescriptionUseNotNull(boolean newValue) {
        boolean old = this.descriptionUseNotNull;
        this.descriptionUseNotNull = newValue;
        firePropertyChange(P_DescriptionUseNotNull, old, this.descriptionUseNotNull);
    }
    @OAProperty(lowerName = "price", decimalPlaces = 2, isCurrency = true, displayLength = 9, uiColumnLength = 8)
    public double getPrice() {
        return price;
    }
    public void setPrice(double newValue) {
        double old = price;
        fireBeforePropertyChange(P_Price, old, newValue);
        this.price = newValue;
        firePropertyChange(P_Price, old, this.price);
        if (isLoading()) return;
        if (price > price2) setPrice2(this.price);
    } 
    public double getPrice2() {
        return price2;
    }
    public void setPrice2(double newValue) {
        double old = price2;
        fireBeforePropertyChange(P_Price2, old, newValue);
        this.price2 = newValue;
        firePropertyChange(P_Price2, old, this.price2);
        if (isLoading()) return;
        if (price > price2) setPrice(this.price2);
    }

    public String getCustomQuery() {
        return customQuery;
    }
    public void setCustomQuery(String newValue) {
        fireBeforePropertyChange(P_CustomQuery, this.customQuery, newValue);
        String old = customQuery;
        this.customQuery = newValue;
        firePropertyChange(P_CustomQuery, old, this.customQuery);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public int getSortByType() {
        return this.sortByType;
    }
    public void setSortByType(int newValue) {
        fireBeforePropertyChange(P_SortByType, this.sortByType, newValue);
        int old = this.sortByType;
        this.sortByType = newValue;
        firePropertyChange(P_SortByType, old, this.sortByType);
    }

    public boolean getSortByDesc() {
        return this.sortByDesc;
    }
    public void setSortByDesc(boolean newValue) {
        fireBeforePropertyChange(P_SortByDesc, this.sortByDesc, newValue);
        boolean old = this.sortByDesc;
        this.sortByDesc = newValue;
        firePropertyChange(P_SortByDesc, old, this.sortByDesc);
    }

    public void reset() {
        setId(0);
        setNull(P_Id);
        setName(null);
        setNameUseNull(false);
        setNameUseNotNull(false);
        setItemCode(null);
        setItemCodeUseNull(false);
        setItemCodeUseNotNull(false);
        setDescription(null);
        setDescriptionUseNull(false);
        setDescriptionUseNotNull(false);
        setPrice(0);
        setNull(P_Price);
        setPrice2(0);
        setNull(P_Price2);
        setCustomQuery(null);
        setSortByType(SORTBYTYPE_None);
        setNull(P_SortByType);
        setSortByDesc(false);
        setNull(P_SortByDesc);
    }

    public boolean isDataEntered() {
        if (!isNull(P_Id)) return true;
        if (getName() != null) return true;
        if (getNameUseNull()) return true;
        if (getNameUseNotNull()) return true;
        if (getItemCode() != null) return true;
        if (getItemCodeUseNull()) return true;
        if (getItemCodeUseNotNull()) return true;
        if (getDescription() != null) return true;
        if (getDescriptionUseNull()) return true;
        if (getDescriptionUseNotNull()) return true;
        if (!isNull(P_Price)) return true;
        if (getCustomQuery() != null) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ServiceCode> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ServiceCode> f = new OAQueryFilter<ServiceCode>(ServiceCode.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ServiceCode> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ServiceCode> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ServiceCode> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];

        switch (getSortByType()) {
            case SORTBYTYPE_None: break;
            case SORTBYTYPE_name: sortOrder = ServiceCode.P_Name; break;
            case SORTBYTYPE_id: sortOrder = ServiceCode.P_Id; break;
            case SORTBYTYPE_description: sortOrder = ServiceCode.P_Description; break;
            case SORTBYTYPE_price: sortOrder = ServiceCode.P_Price; break;
            case SORTBYTYPE_itemCode: sortOrder = ServiceCode.P_ItemCode; break;
        }
        if (OAString.isNotEmpty(sortOrder) && getSortByDesc()) sortOrder += " DESC";

        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServiceCode.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + ServiceCode.P_Name + " = null OR " + ServiceCode.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServiceCode.P_Name + " != null";
        }
        else if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += ServiceCode.P_Name + " LIKE ?";
            }
            else {
                sql += ServiceCode.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (itemCodeUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + ServiceCode.P_ItemCode + " = null OR " + ServiceCode.P_ItemCode + " == '')";
        }
        else if (itemCodeUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServiceCode.P_ItemCode + " != null";
        }
        else if (OAString.isNotEmpty(this.itemCode)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(itemCode);
            if (val.indexOf("%") >= 0) {
                sql += ServiceCode.P_ItemCode + " LIKE ?";
            }
            else {
                sql += ServiceCode.P_ItemCode + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (descriptionUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + ServiceCode.P_Description + " = null OR " + ServiceCode.P_Description + " == '')";
        }
        else if (descriptionUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ServiceCode.P_Description + " != null";
        }
        else if (OAString.isNotEmpty(this.description)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(description);
            if (val.indexOf("%") >= 0) {
                sql += ServiceCode.P_Description + " LIKE ?";
            }
            else {
                sql += ServiceCode.P_Description + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (!isNull(P_Price)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Price2) && price != price2) {
                sql += ServiceCode.P_Price + " >= ?";
                args = OAArray.add(Object.class, args, getPrice());
                sql += " AND " + ServiceCode.P_Price + " <= ?";
                args = OAArray.add(Object.class, args, getPrice2());
            }
            else {
                sql += ServiceCode.P_Price + " = ?";
                args = OAArray.add(Object.class, args, getPrice());
            }
        }
    if (OAString.isNotEmpty(this.customQuery)) {
        if (sql.length() > 0) sql += " AND ";
        sql += "(" + getCustomQuery() + ")";
    }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ServiceCode> select = new OASelect<ServiceCode>(ServiceCode.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServiceCode.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + prefix + ServiceCode.P_Name + " = null OR " + prefix + ServiceCode.P_Name + " == '')";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServiceCode.P_Name + " != null";
        }
        else if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(name);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ServiceCode.P_Name + " LIKE ?";
            }
            else {
                sql += prefix + ServiceCode.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (itemCodeUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + prefix + ServiceCode.P_ItemCode + " = null OR " + prefix + ServiceCode.P_ItemCode + " == '')";
        }
        else if (itemCodeUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServiceCode.P_ItemCode + " != null";
        }
        else if (OAString.isNotEmpty(this.itemCode)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(itemCode);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ServiceCode.P_ItemCode + " LIKE ?";
            }
            else {
                sql += prefix + ServiceCode.P_ItemCode + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (descriptionUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + prefix + ServiceCode.P_Description + " = null OR " + prefix + ServiceCode.P_Description + " == '')";
        }
        else if (descriptionUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ServiceCode.P_Description + " != null";
        }
        else if (OAString.isNotEmpty(this.description)) {
            if (sql.length() > 0) sql += " AND ";
            String val = OAString.convertToLikeSearch(description);
            if (val.indexOf("%") >= 0) {
                sql += prefix + ServiceCode.P_Description + " LIKE ?";
            }
            else {
                sql += prefix + ServiceCode.P_Description + " = ?";
            }
            args = OAArray.add(Object.class, args, val);
        }
        if (!isNull(P_Price)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Price2) && price != price2) {
                sql += prefix + ServiceCode.P_Price + " >= ?";
                args = OAArray.add(Object.class, args, getPrice());
                sql += " AND " + prefix + ServiceCode.P_Price + " <= ?";
                args = OAArray.add(Object.class, args, getPrice2());
            }
            else {
                sql += prefix + ServiceCode.P_Price + " = ?";
                args = OAArray.add(Object.class, args, getPrice());
            }
        }
    if (OAString.isNotEmpty(this.customQuery)) {
        if (sql.length() > 0) sql += " AND ";
        sql += "(" + getCustomQuery() + ")";
    }
        select.add(sql, args);
    }

    private OAFilter<ServiceCode> filterDataSourceFilter;
    public OAFilter<ServiceCode> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ServiceCode>() {
            @Override
            public boolean isUsed(ServiceCode serviceCode) {
                return ServiceCodeSearch.this.isUsedForDataSourceFilter(serviceCode);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ServiceCode> filterCustomFilter;
    public OAFilter<ServiceCode> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ServiceCode>() {
            @Override
            public boolean isUsed(ServiceCode serviceCode) {
                boolean b = ServiceCodeSearch.this.isUsedForCustomFilter(serviceCode);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ServiceCode searchServiceCode) {
        return true;
    }
    public boolean isUsedForCustomFilter(ServiceCode searchServiceCode) {
        return true;
    }
}
