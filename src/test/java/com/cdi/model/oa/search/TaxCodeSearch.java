// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.ds.*;
import com.viaoa.util.filter.OAQueryFilter;

@OAClass(useDataSource=false, localOnly=true)
public class TaxCodeSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(TaxCodeSearch.class.getName());
    public static final String P_Id = "Id";
    public static final String P_Code = "Code";
    public static final String P_CodeUseNull = "CodeUseNull";
    public static final String P_CodeUseNotNull = "CodeUseNotNull";
    public static final String P_TaxRate = "TaxRate";
    public static final String P_TaxRate2 = "TaxRate2";
    public static final String P_MaxResults = "MaxResults";
    public static final String P_SortByType = "SortByType";
    public static final String P_SortByDesc = "SortByDesc";

    protected int id;
    protected String code;
    protected boolean codeUseNull;
    protected boolean codeUseNotNull;
    protected double taxRate;
    protected double taxRate2;
    protected int maxResults;
    protected int sortByType;
    protected boolean sortByDesc;

    public static final int SORTBYTYPE_None = 0;
    public static final int SORTBYTYPE_code = 1;
    public static final int SORTBYTYPE_taxRate = 2;
    public static final Hub<String> hubSortByType;
    static {
        hubSortByType = new Hub<String>(String.class);
        hubSortByType.add("None");
        hubSortByType.add("Code");
        hubSortByType.add("Tax Rate");
    }

    public TaxCodeSearch() {
        reset();
    }

    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
      

    public String getCode() {
        return code;
    }
    public void setCode(String newValue) {
        String old = code;
        fireBeforePropertyChange(P_Code, old, newValue);
        this.code = newValue;
        firePropertyChange(P_Code, old, this.code);
    }
      
    public boolean getCodeUseNull() {
        return codeUseNull;
    }
    public void setCodeUseNull(boolean newValue) {
        boolean old = this.codeUseNull;
        this.codeUseNull = newValue;
        firePropertyChange(P_CodeUseNull, old, this.codeUseNull);
    }
    public boolean getCodeUseNotNull() {
        return codeUseNotNull;
    }
    public void setCodeUseNotNull(boolean newValue) {
        boolean old = this.codeUseNotNull;
        this.codeUseNotNull = newValue;
        firePropertyChange(P_CodeUseNotNull, old, this.codeUseNotNull);
    }

    public double getTaxRate() {
        return taxRate;
    }
    public void setTaxRate(double newValue) {
        double old = taxRate;
        fireBeforePropertyChange(P_TaxRate, old, newValue);
        this.taxRate = newValue;
        firePropertyChange(P_TaxRate, old, this.taxRate);
        if (isLoading()) return;
        if (taxRate > taxRate2) setTaxRate2(this.taxRate);
    } 
    public double getTaxRate2() {
        return taxRate2;
    }
    public void setTaxRate2(double newValue) {
        double old = taxRate2;
        fireBeforePropertyChange(P_TaxRate2, old, newValue);
        this.taxRate2 = newValue;
        firePropertyChange(P_TaxRate2, old, this.taxRate2);
        if (isLoading()) return;
        if (taxRate > taxRate2) setTaxRate(this.taxRate2);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public int getSortByType() {
        return this.sortByType;
    }
    public void setSortByType(int newValue) {
        fireBeforePropertyChange(P_SortByType, this.sortByType, newValue);
        int old = this.sortByType;
        this.sortByType = newValue;
        firePropertyChange(P_SortByType, old, this.sortByType);
    }

    public boolean getSortByDesc() {
        return this.sortByDesc;
    }
    public void setSortByDesc(boolean newValue) {
        fireBeforePropertyChange(P_SortByDesc, this.sortByDesc, newValue);
        boolean old = this.sortByDesc;
        this.sortByDesc = newValue;
        firePropertyChange(P_SortByDesc, old, this.sortByDesc);
    }

    public void reset() {
        setId(0);
        setNull(P_Id);
        setCode(null);
        setCodeUseNull(false);
        setCodeUseNotNull(false);
        setTaxRate(0);
        setNull(P_TaxRate);
        setTaxRate2(0);
        setNull(P_TaxRate2);
        setSortByType(SORTBYTYPE_None);
        setNull(P_SortByType);
        setSortByDesc(false);
        setNull(P_SortByDesc);
    }

    public boolean isDataEntered() {
        if (!isNull(P_Id)) return true;
        if (getCode() != null) return true;
        if (getCodeUseNull()) return true;if (getCodeUseNotNull()) return true;
        if (getCodeUseNull()) return true;
        if (getCodeUseNotNull()) return true;

        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<TaxCode> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<TaxCode> f = new OAQueryFilter<TaxCode>(TaxCode.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<TaxCode> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<TaxCode> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<TaxCode> getSelect() {
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];

        switch (getSortByType()) {
            case SORTBYTYPE_None: break;
            case SORTBYTYPE_code: sortOrder = TaxCode.P_Code; break;
            case SORTBYTYPE_taxRate: sortOrder = TaxCode.P_TaxRate; break;
        }
        if (OAString.isNotEmpty(sortOrder) && getSortByDesc()) sortOrder += " DESC";

        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += TaxCode.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (codeUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + TaxCode.P_Code + " = null OR " + TaxCode.P_Code + " == '')";
        }
        else if (codeUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += TaxCode.P_Code + " != null";
        }
        else if (!OAString.isEmpty(this.code)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.code.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += TaxCode.P_Code + " LIKE ?";
            }
            else {
                sql += TaxCode.P_Code + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!isNull(P_TaxRate)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_TaxRate2) && taxRate != taxRate2) {
                sql += TaxCode.P_TaxRate + " >= ?";
                args = OAArray.add(Object.class, args, getTaxRate());
                sql += " AND " + TaxCode.P_TaxRate + " <= ?";
                args = OAArray.add(Object.class, args, getTaxRate2());
            }
            else {
                sql += TaxCode.P_TaxRate + " = ?";
                args = OAArray.add(Object.class, args, getTaxRate());
            }
        }

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<TaxCode> select = new OASelect<TaxCode>(TaxCode.class, sql, args, sortOrder);
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFilter(this.getCustomFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!isNull(P_Id)) {
            if (sql.length() > 0) sql += " AND ";
            sql += TaxCode.P_Id + " = ?";
            args = OAArray.add(Object.class, args, this.id);
        }
        if (codeUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += "(" + TaxCode.P_Code + " = null OR " + TaxCode.P_Code + " == '')";
        }
        else if (codeUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += TaxCode.P_Code + " != null";
        }
        else if (!OAString.isEmpty(this.code)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.code.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += TaxCode.P_Code + " LIKE ?";
            }
            else {
                sql += TaxCode.P_Code + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!isNull(P_TaxRate)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_TaxRate2) && taxRate != taxRate2) {
                sql += TaxCode.P_TaxRate + " >= ?";
                args = OAArray.add(Object.class, args, getTaxRate());
                sql += " AND " + TaxCode.P_TaxRate + " <= ?";
                args = OAArray.add(Object.class, args, getTaxRate2());
            }
            else {
                sql += TaxCode.P_TaxRate + " = ?";
                args = OAArray.add(Object.class, args, getTaxRate());
            }
        }
        select.add(sql, args);
    }

    private OAFilter<TaxCode> filterDataSourceFilter;
    public OAFilter<TaxCode> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<TaxCode>() {
            @Override
            public boolean isUsed(TaxCode taxCode) {
                return TaxCodeSearch.this.isUsedForDataSourceFilter(taxCode);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<TaxCode> filterCustomFilter;
    public OAFilter<TaxCode> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<TaxCode>() {
            @Override
            public boolean isUsed(TaxCode taxCode) {
                boolean b = TaxCodeSearch.this.isUsedForCustomFilter(taxCode);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(taxCode);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(TaxCode searchTaxCode) {
        if (!isNull(P_Id)) {
            if (!OACompare.isEqual(searchTaxCode.getId(), id)) return false;
        }
        if (codeUseNull) {
            if (OACompare.isNotEmpty(searchTaxCode.getCode())) return false;
        }
        else if (codeUseNotNull) {
            if (OACompare.isEmpty(searchTaxCode.getCode())) return false;
        }
        else if (code != null) {
            String s = getCode();
            if (s != null && s.indexOf('*') < 0 && s.indexOf('%') < 0) s += '*';
            if (!OACompare.isLike(searchTaxCode.getCode(), s)) return false;
        }
        if (!isNull(P_TaxRate2)) {
            if (!OACompare.isEqualOrBetween(searchTaxCode.getTaxRate(), taxRate, taxRate2)) return false;
        }
        return true;
    }
    public boolean isUsedForCustomFilter(TaxCode searchTaxCode) {
        return true;
    }
}
