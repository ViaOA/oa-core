// Generated by OABuilder
package com.cdi.model.oa.search;

import java.util.logging.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.datasource.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.OADate;
import com.viaoa.filter.OAQueryFilter;

@OAClass(useDataSource=false, localOnly=true)
public class WebPartSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(WebPartSearch.class.getName());
    public static final String P_Created = "Created";
    public static final String P_Title = "Title";
    public static final String P_MaxResults = "MaxResults";

    protected OADate created;
    protected String title;
    protected int maxResults;

    public OADate getCreated() {
        return created;
    }
    public void setCreated(OADate newValue) {
        OADate old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }
      

    public String getTitle() {
        return title;
    }
    public void setTitle(String newValue) {
        String old = title;
        fireBeforePropertyChange(P_Title, old, newValue);
        this.title = newValue;
        firePropertyChange(P_Title, old, this.title);
    }
      

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    public void reset() {
        setCreated(null);
        setTitle(null);
    }

    public boolean isDataEntered() {
        if (getCreated() != null) return true;
        if (getTitle() != null) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<WebPart> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<WebPart> f = new OAQueryFilter<WebPart>(WebPart.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<WebPart> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<WebPart> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<WebPart> getSelect() {
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += WebPart.P_Created + " = ?";
            args = OAArray.add(Object.class, args, this.created);
        }
        if (!OAString.isEmpty(this.title)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.title.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += WebPart.P_Title + " LIKE ?";
            }
            else {
                sql += WebPart.P_Title + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<WebPart> select = new OASelect<WebPart>(WebPart.class, sql, args, sortOrder);
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFilter(this.getCustomFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += WebPart.P_Created + " = ?";
            args = OAArray.add(Object.class, args, this.created);
        }
        if (!OAString.isEmpty(this.title)) {
            if (sql.length() > 0) sql += " AND ";
            String value = this.title.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                sql += WebPart.P_Title + " LIKE ?";
            }
            else {
                sql += WebPart.P_Title + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        select.add(sql, args);
    }

    private OAFilter<WebPart> filterDataSourceFilter;
    public OAFilter<WebPart> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<WebPart>() {
            @Override
            public boolean isUsed(WebPart webPart) {
                return WebPartSearch.this.isUsedForDataSourceFilter(webPart);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<WebPart> filterCustomFilter;
    public OAFilter<WebPart> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<WebPart>() {
            @Override
            public boolean isUsed(WebPart webPart) {
                boolean b = WebPartSearch.this.isUsedForCustomFilter(webPart);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(webPart);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(WebPart searchWebPart) {
        if (created != null) {
            if (!OACompare.isEqual(searchWebPart.getCreated(), created)) return false;
        }
        if (title != null) {
            String s = getTitle();
            if (s != null && s.indexOf('*') < 0 && s.indexOf('%') < 0) s += '*';
            if (!OACompare.isLike(searchWebPart.getTitle(), s)) return false;
        }
        return true;
    }
    public boolean isUsedForCustomFilter(WebPart searchWebPart) {
        return true;
    }
}
