// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.viaoa.ds.*;

import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class CountrySearchModel {
    private static Logger LOG = Logger.getLogger(CountrySearchModel.class.getName());
    
    protected Hub<Country> hub;  // search results
    protected Hub<Country> hubMultiSelect;
    protected Hub<Country> hubSearchFrom;  // hub (optional) to search from
    protected Hub<CountrySearch> hubCountrySearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Country> finder;
    
    // object used for search data
    protected CountrySearch searchCountry;
    
    public CountrySearchModel() {
    }
    
    public CountrySearchModel(Hub<Country> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Country> getHub() {
        if (hub == null) {
            hub = new Hub<Country>(Country.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Country> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Country> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    CountrySearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Country> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Country.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Country> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Country> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public CountrySearch getCountrySearch() {
        if (searchCountry != null) return searchCountry;
        searchCountry = new CountrySearch();
        return searchCountry;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<CountrySearch> getCountrySearchHub() {
        if (hubCountrySearch == null) {
            hubCountrySearch = new Hub<CountrySearch>(CountrySearch.class);
            hubCountrySearch.add(getCountrySearch());
            hubCountrySearch.setPos(0);
        }
        return hubCountrySearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses CountrySearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Country> sel = getCountrySearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Country country, Hub<Country> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Country> hub) {
    }
}

