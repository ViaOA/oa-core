// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class ItemCategorySearchModel {
    private static Logger LOG = Logger.getLogger(ItemCategorySearchModel.class.getName());
    
    protected Hub<ItemCategory> hub;  // search results
    protected Hub<ItemCategory> hubMultiSelect;
    protected Hub<ItemCategory> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ItemCategorySearch> hubItemCategorySearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ItemCategory> finder;
    
    // object used for search data
    protected ItemCategorySearch searchItemCategory;
    
    public ItemCategorySearchModel() {
    }
    
    public ItemCategorySearchModel(Hub<ItemCategory> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ItemCategory> getHub() {
        if (hub == null) {
            hub = new Hub<ItemCategory>(ItemCategory.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ItemCategory> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ItemCategory> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ItemCategorySearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ItemCategory> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ItemCategory.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ItemCategory> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ItemCategory> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ItemCategorySearch getItemCategorySearch() {
        if (searchItemCategory != null) return searchItemCategory;
        searchItemCategory = new ItemCategorySearch();
        return searchItemCategory;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ItemCategorySearch> getItemCategorySearchHub() {
        if (hubItemCategorySearch == null) {
            hubItemCategorySearch = new Hub<ItemCategorySearch>(ItemCategorySearch.class);
            hubItemCategorySearch.add(getItemCategorySearch());
            hubItemCategorySearch.setPos(0);
        }
        return hubItemCategorySearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ItemCategorySearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ItemCategory> sel = getItemCategorySearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ItemCategory itemCategory, Hub<ItemCategory> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ItemCategory> hub) {
    }
}

