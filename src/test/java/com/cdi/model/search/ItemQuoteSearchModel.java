// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class ItemQuoteSearchModel {
    private static Logger LOG = Logger.getLogger(ItemQuoteSearchModel.class.getName());
    
    protected Hub<ItemQuote> hub;  // search results
    protected Hub<ItemQuote> hubMultiSelect;
    protected Hub<ItemQuote> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ItemQuoteSearch> hubItemQuoteSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ItemQuote> finder;
    
    // object used for search data
    protected ItemQuoteSearch searchItemQuote;
    
    public ItemQuoteSearchModel() {
    }
    
    public ItemQuoteSearchModel(Hub<ItemQuote> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ItemQuote> getHub() {
        if (hub == null) {
            hub = new Hub<ItemQuote>(ItemQuote.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ItemQuote> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ItemQuote> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ItemQuoteSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ItemQuote> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ItemQuote.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ItemQuote> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ItemQuote> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ItemQuoteSearch getItemQuoteSearch() {
        if (searchItemQuote != null) return searchItemQuote;
        searchItemQuote = new ItemQuoteSearch();
        return searchItemQuote;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ItemQuoteSearch> getItemQuoteSearchHub() {
        if (hubItemQuoteSearch == null) {
            hubItemQuoteSearch = new Hub<ItemQuoteSearch>(ItemQuoteSearch.class);
            hubItemQuoteSearch.add(getItemQuoteSearch());
            hubItemQuoteSearch.setPos(0);
        }
        return hubItemQuoteSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ItemQuoteSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ItemQuote> sel = getItemQuoteSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ItemQuote itemQuote, Hub<ItemQuote> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ItemQuote> hub) {
    }
}

