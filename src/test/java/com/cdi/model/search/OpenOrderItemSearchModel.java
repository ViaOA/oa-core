// Generated by OABuilder

package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class OpenOrderItemSearchModel {
    private static Logger LOG = Logger.getLogger(OpenOrderItemSearchModel.class.getName());
    
    protected Hub<OpenOrderItem> hub;  // search results
    protected Hub<OpenOrderItem> hubMultiSelect;
    protected Hub<OpenOrderItem> hubSearchFrom;  // hub (optional) to search from
    protected Hub<OpenOrderItemSearch> hubOpenOrderItemSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, OpenOrderItem> finder;
    
    // object used for search data
    protected OpenOrderItemSearch openOrderItemSearch;
    
    public OpenOrderItemSearchModel() {
    }
    
    public OpenOrderItemSearchModel(Hub<OpenOrderItem> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<OpenOrderItem> getHub() {
        if (hub == null) {
            hub = new Hub<OpenOrderItem>(OpenOrderItem.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<OpenOrderItem> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<OpenOrderItem> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    OpenOrderItemSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<OpenOrderItem> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(OpenOrderItem.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, OpenOrderItem> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, OpenOrderItem> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public OpenOrderItemSearch getOpenOrderItemSearch() {
        if (openOrderItemSearch != null) return openOrderItemSearch;
        openOrderItemSearch = new OpenOrderItemSearch();
        return openOrderItemSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<OpenOrderItemSearch> getOpenOrderItemSearchHub() {
        if (hubOpenOrderItemSearch == null) {
            hubOpenOrderItemSearch = new Hub<OpenOrderItemSearch>(OpenOrderItemSearch.class);
            hubOpenOrderItemSearch.add(getOpenOrderItemSearch());
            hubOpenOrderItemSearch.setPos(0);
        }
        return hubOpenOrderItemSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses OpenOrderItemSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<OpenOrderItem> sel = getOpenOrderItemSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(OpenOrderItem openOrderItem, Hub<OpenOrderItem> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<OpenOrderItem> hub) {
    }
}

