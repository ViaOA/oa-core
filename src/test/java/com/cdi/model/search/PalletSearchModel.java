// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.viaoa.ds.*;

import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class PalletSearchModel {
    private static Logger LOG = Logger.getLogger(PalletSearchModel.class.getName());
    
    protected Hub<Pallet> hub;  // search results
    protected Hub<Pallet> hubMultiSelect;
    protected Hub<Pallet> hubSearchFrom;  // hub (optional) to search from
    protected Hub<PalletSearch> hubPalletSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Pallet> finder;
    
    // object used for search data
    protected PalletSearch searchPallet;
    
    public PalletSearchModel() {
    }
    
    public PalletSearchModel(Hub<Pallet> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Pallet> getHub() {
        if (hub == null) {
            hub = new Hub<Pallet>(Pallet.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Pallet> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Pallet> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    PalletSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Pallet> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Pallet.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Pallet> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Pallet> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public PalletSearch getPalletSearch() {
        if (searchPallet != null) return searchPallet;
        searchPallet = new PalletSearch();
        return searchPallet;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<PalletSearch> getPalletSearchHub() {
        if (hubPalletSearch == null) {
            hubPalletSearch = new Hub<PalletSearch>(PalletSearch.class);
            hubPalletSearch.add(getPalletSearch());
            hubPalletSearch.setPos(0);
        }
        return hubPalletSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses PalletSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Pallet> sel = getPalletSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Pallet pallet, Hub<Pallet> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Pallet> hub) {
    }
}

