// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class RegionSearchModel {
    private static Logger LOG = Logger.getLogger(RegionSearchModel.class.getName());
    
    protected Hub<Region> hub;  // search results
    protected Hub<Region> hubMultiSelect;
    protected Hub<Region> hubSearchFrom;  // hub (optional) to search from
    protected Hub<RegionSearch> hubRegionSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Region> finder;
    
    // object used for search data
    protected RegionSearch searchRegion;
    
    public RegionSearchModel() {
    }
    
    public RegionSearchModel(Hub<Region> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Region> getHub() {
        if (hub == null) {
            hub = new Hub<Region>(Region.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Region> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Region> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    RegionSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Region> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Region.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Region> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Region> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public RegionSearch getRegionSearch() {
        if (searchRegion != null) return searchRegion;
        searchRegion = new RegionSearch();
        return searchRegion;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<RegionSearch> getRegionSearchHub() {
        if (hubRegionSearch == null) {
            hubRegionSearch = new Hub<RegionSearch>(RegionSearch.class);
            hubRegionSearch.add(getRegionSearch());
            hubRegionSearch.setPos(0);
        }
        return hubRegionSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses RegionSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Region> sel = getRegionSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Region region, Hub<Region> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Region> hub) {
    }
}

