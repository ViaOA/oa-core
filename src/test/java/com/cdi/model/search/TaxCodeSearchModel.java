// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class TaxCodeSearchModel {
    private static Logger LOG = Logger.getLogger(TaxCodeSearchModel.class.getName());
    
    protected Hub<TaxCode> hub;  // search results
    protected Hub<TaxCode> hubMultiSelect;
    protected Hub<TaxCode> hubSearchFrom;  // hub (optional) to search from
    protected Hub<TaxCodeSearch> hubTaxCodeSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, TaxCode> finder;
    
    // object used for search data
    protected TaxCodeSearch searchTaxCode;
    
    public TaxCodeSearchModel() {
    }
    
    public TaxCodeSearchModel(Hub<TaxCode> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<TaxCode> getHub() {
        if (hub == null) {
            hub = new Hub<TaxCode>(TaxCode.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<TaxCode> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<TaxCode> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    TaxCodeSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<TaxCode> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(TaxCode.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, TaxCode> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, TaxCode> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public TaxCodeSearch getTaxCodeSearch() {
        if (searchTaxCode != null) return searchTaxCode;
        searchTaxCode = new TaxCodeSearch();
        return searchTaxCode;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<TaxCodeSearch> getTaxCodeSearchHub() {
        if (hubTaxCodeSearch == null) {
            hubTaxCodeSearch = new Hub<TaxCodeSearch>(TaxCodeSearch.class);
            hubTaxCodeSearch.add(getTaxCodeSearch());
            hubTaxCodeSearch.setPos(0);
        }
        return hubTaxCodeSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses TaxCodeSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<TaxCode> sel = getTaxCodeSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(TaxCode taxCode, Hub<TaxCode> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<TaxCode> hub) {
    }
}

