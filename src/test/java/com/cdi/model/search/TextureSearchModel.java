// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.viaoa.ds.*;

import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class TextureSearchModel {
    private static Logger LOG = Logger.getLogger(TextureSearchModel.class.getName());
    
    protected Hub<Texture> hub;  // search results
    protected Hub<Texture> hubMultiSelect;
    protected Hub<Texture> hubSearchFrom;  // hub (optional) to search from
    protected Hub<TextureSearch> hubTextureSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Texture> finder;
    
    // object used for search data
    protected TextureSearch searchTexture;
    
    public TextureSearchModel() {
    }
    
    public TextureSearchModel(Hub<Texture> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Texture> getHub() {
        if (hub == null) {
            hub = new Hub<Texture>(Texture.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Texture> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Texture> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    TextureSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Texture> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Texture.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Texture> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Texture> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public TextureSearch getTextureSearch() {
        if (searchTexture != null) return searchTexture;
        searchTexture = new TextureSearch();
        return searchTexture;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<TextureSearch> getTextureSearchHub() {
        if (hubTextureSearch == null) {
            hubTextureSearch = new Hub<TextureSearch>(TextureSearch.class);
            hubTextureSearch.add(getTextureSearch());
            hubTextureSearch.setPos(0);
        }
        return hubTextureSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses TextureSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Texture> sel = getTextureSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Texture texture, Hub<Texture> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Texture> hub) {
    }
}

