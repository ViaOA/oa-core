// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class TruckSearchModel {
    private static Logger LOG = Logger.getLogger(TruckSearchModel.class.getName());
    
    protected Hub<Truck> hub;  // search results
    protected Hub<Truck> hubMultiSelect;
    protected Hub<Truck> hubSearchFrom;  // hub (optional) to search from
    protected Hub<TruckSearch> hubTruckSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Truck> finder;
    
    // object used for search data
    protected TruckSearch searchTruck;
    
    public TruckSearchModel() {
    }
    
    public TruckSearchModel(Hub<Truck> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Truck> getHub() {
        if (hub == null) {
            hub = new Hub<Truck>(Truck.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Truck> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Truck> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    TruckSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Truck> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Truck.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Truck> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Truck> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public TruckSearch getTruckSearch() {
        if (searchTruck != null) return searchTruck;
        searchTruck = new TruckSearch();
        return searchTruck;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<TruckSearch> getTruckSearchHub() {
        if (hubTruckSearch == null) {
            hubTruckSearch = new Hub<TruckSearch>(TruckSearch.class);
            hubTruckSearch.add(getTruckSearch());
            hubTruckSearch.setPos(0);
        }
        return hubTruckSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses TruckSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Truck> sel = getTruckSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Truck truck, Hub<Truck> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Truck> hub) {
    }
}

