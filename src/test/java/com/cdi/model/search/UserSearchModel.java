// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class UserSearchModel {
    private static Logger LOG = Logger.getLogger(UserSearchModel.class.getName());
    
    protected Hub<User> hub;  // search results
    protected Hub<User> hubMultiSelect;
    protected Hub<User> hubSearchFrom;  // hub (optional) to search from
    protected Hub<UserSearch> hubUserSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, User> finder;
    
    // object used for search data
    protected UserSearch searchUser;
    
    public UserSearchModel() {
    }
    
    public UserSearchModel(Hub<User> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<User> getHub() {
        if (hub == null) {
            hub = new Hub<User>(User.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<User> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<User> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    UserSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<User> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(User.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, User> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, User> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public UserSearch getUserSearch() {
        if (searchUser != null) return searchUser;
        searchUser = new UserSearch();
        return searchUser;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<UserSearch> getUserSearchHub() {
        if (hubUserSearch == null) {
            hubUserSearch = new Hub<UserSearch>(UserSearch.class);
            hubUserSearch.add(getUserSearch());
            hubUserSearch.setPos(0);
        }
        return hubUserSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses UserSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<User> sel = getUserSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(User user, Hub<User> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<User> hub) {
    }
}

