// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class WebPageSearchModel {
    private static Logger LOG = Logger.getLogger(WebPageSearchModel.class.getName());
    
    protected Hub<WebPage> hub;  // search results
    protected Hub<WebPage> hubMultiSelect;
    protected Hub<WebPage> hubSearchFrom;  // hub (optional) to search from
    protected Hub<WebPageSearch> hubWebPageSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, WebPage> finder;
    
    // object used for search data
    protected WebPageSearch searchWebPage;
    
    public WebPageSearchModel() {
    }
    
    public WebPageSearchModel(Hub<WebPage> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<WebPage> getHub() {
        if (hub == null) {
            hub = new Hub<WebPage>(WebPage.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<WebPage> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<WebPage> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    WebPageSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<WebPage> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(WebPage.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, WebPage> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, WebPage> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public WebPageSearch getWebPageSearch() {
        if (searchWebPage != null) return searchWebPage;
        searchWebPage = new WebPageSearch();
        return searchWebPage;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<WebPageSearch> getWebPageSearchHub() {
        if (hubWebPageSearch == null) {
            hubWebPageSearch = new Hub<WebPageSearch>(WebPageSearch.class);
            hubWebPageSearch.add(getWebPageSearch());
            hubWebPageSearch.setPos(0);
        }
        return hubWebPageSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses WebPageSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<WebPage> sel = getWebPageSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(WebPage webPage, Hub<WebPage> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<WebPage> hub) {
    }
}

