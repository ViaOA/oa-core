// Generated by OABuilder
package com.cdi.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.cdi.model.*;
import com.cdi.model.oa.*;
import com.cdi.model.oa.propertypath.*;
import com.cdi.model.oa.search.*;
import com.cdi.model.oa.filter.*;
import com.cdi.delegate.ModelDelegate;
import com.cdi.resource.Resource;

public class WebPartSearchModel {
    private static Logger LOG = Logger.getLogger(WebPartSearchModel.class.getName());
    
    protected Hub<WebPart> hub;  // search results
    protected Hub<WebPart> hubMultiSelect;
    protected Hub<WebPart> hubSearchFrom;  // hub (optional) to search from
    protected Hub<WebPartSearch> hubWebPartSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, WebPart> finder;
    
    // object used for search data
    protected WebPartSearch searchWebPart;
    
    public WebPartSearchModel() {
    }
    
    public WebPartSearchModel(Hub<WebPart> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<WebPart> getHub() {
        if (hub == null) {
            hub = new Hub<WebPart>(WebPart.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<WebPart> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<WebPart> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    WebPartSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<WebPart> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(WebPart.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, WebPart> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, WebPart> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public WebPartSearch getWebPartSearch() {
        if (searchWebPart != null) return searchWebPart;
        searchWebPart = new WebPartSearch();
        return searchWebPart;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<WebPartSearch> getWebPartSearchHub() {
        if (hubWebPartSearch == null) {
            hubWebPartSearch = new Hub<WebPartSearch>(WebPartSearch.class);
            hubWebPartSearch.add(getWebPartSearch());
            hubWebPartSearch.setPos(0);
        }
        return hubWebPartSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses WebPartSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<WebPart> sel = getWebPartSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(WebPart webPart, Hub<WebPart> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<WebPart> hub) {
    }
}

