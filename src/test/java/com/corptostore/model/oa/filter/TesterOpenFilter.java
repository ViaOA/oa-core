// Generated by OABuilder
package com.corptostore.model.oa.filter;

import java.util.*;
import java.util.logging.*;

import com.corptostore.model.oa.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.model.search.*;
import com.corptostore.model.oa.Tester;
import com.corptostore.model.oa.filter.TesterOpenFilter;
import com.corptostore.model.oa.propertypath.TesterPP;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import java.util.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "Open", displayName = "Open", hasInputParams = false)
public class TesterOpenFilter extends OAObject implements CustomHubFilter<Tester> {
    private static final long serialVersionUID = 1L;

    private static Logger LOG = Logger.getLogger(TesterOpenFilter.class.getName());


    public static final String PPCode = ":Open()";
    private Hub<Tester> hubMaster;
    private Hub<Tester> hub;
    private HubFilter<Tester> hubFilter;
    private OAObjectCacheFilter<Tester> cacheFilter;
    private boolean bUseObjectCache;

    public TesterOpenFilter() {
        this(null, null, false);
    }
    public TesterOpenFilter(Hub<Tester> hub) {
        this(null, hub, true);
    }
    public TesterOpenFilter(Hub<Tester> hubMaster, Hub<Tester> hub) {
        this(hubMaster, hub, false);
    }
    public TesterOpenFilter(Hub<Tester> hubMaster, Hub<Tester> hubFiltered, boolean bUseObjectCache) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        this.bUseObjectCache = bUseObjectCache;
        if (hubMaster != null) getHubFilter();
        if (bUseObjectCache) getObjectCacheFilter();
    }



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (hubFilter != null) getHubFilter().refresh();
        if (cacheFilter != null) getObjectCacheFilter().refresh();
    }

    @Override
    public HubFilter<Tester> getHubFilter() {
        if (hubFilter != null) return hubFilter;
        if (hubMaster == null) return null;
        hubFilter = new HubFilter<Tester>(hubMaster, hub) {
            @Override
            public boolean isUsed(Tester tester) {
                return TesterOpenFilter.this.isUsed(tester);
            }
        };
        hubFilter.addDependentProperty(TesterPP.isTestDone(), false);
        hubFilter.refresh();
        return hubFilter;
    }

    public OAObjectCacheFilter<Tester> getObjectCacheFilter() {
        if (cacheFilter != null) return cacheFilter;
        if (!bUseObjectCache) return null;
        cacheFilter = new OAObjectCacheFilter<Tester>(hub) {
            @Override
            public boolean isUsed(Tester tester) {
                return TesterOpenFilter.this.isUsed(tester);
            }
            @Override
            protected void reselect() {
                TesterOpenFilter.this.reselect();
            }
        };
        cacheFilter.addDependentProperty(TesterPP.isTestDone(), false);
        cacheFilter.refresh();
        return cacheFilter;
    }

    public void reselect() {
        // can be overwritten to query datasource
    }

    // ==================
    // this method has custom code that will need to be put into the OABuilder filter

    @Override
    public boolean isUsed(Tester tester) {
        boolean testDone = tester.getIsTestDone();
        return !testDone;
    }
}
