// Generated by OABuilder
package com.corptostore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.corptostore.delegate.ModelDelegate;
import com.corptostore.model.*;
import com.corptostore.model.oa.*;
import com.corptostore.model.oa.filter.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.resource.Resource;
import com.corptostore.model.oa.CorpToStore;
import com.corptostore.model.oa.search.CorpToStoreSearch;
import com.corptostore.model.search.CorpToStoreSearchModel;
import com.viaoa.datasource.*;

public class CorpToStoreSearchModel {
    private static Logger LOG = Logger.getLogger(CorpToStoreSearchModel.class.getName());
    
    protected Hub<CorpToStore> hub;  // search results
    protected Hub<CorpToStore> hubMultiSelect;
    protected Hub<CorpToStore> hubSearchFrom;  // hub (optional) to search from
    protected Hub<CorpToStoreSearch> hubCorpToStoreSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, CorpToStore> finder;
    
    // object used for search data
    protected CorpToStoreSearch corpToStoreSearch;
    
    public CorpToStoreSearchModel() {
    }
    
    public CorpToStoreSearchModel(Hub<CorpToStore> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<CorpToStore> getHub() {
        if (hub == null) {
            hub = new Hub<CorpToStore>(CorpToStore.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<CorpToStore> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<CorpToStore> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    CorpToStoreSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<CorpToStore> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(CorpToStore.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, CorpToStore> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, CorpToStore> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public CorpToStoreSearch getCorpToStoreSearch() {
        if (corpToStoreSearch != null) return corpToStoreSearch;
        corpToStoreSearch = new CorpToStoreSearch();
        return corpToStoreSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<CorpToStoreSearch> getCorpToStoreSearchHub() {
        if (hubCorpToStoreSearch == null) {
            hubCorpToStoreSearch = new Hub<CorpToStoreSearch>(CorpToStoreSearch.class);
            hubCorpToStoreSearch.add(getCorpToStoreSearch());
            hubCorpToStoreSearch.setPos(0);
        }
        return hubCorpToStoreSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses CorpToStoreSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<CorpToStore> sel = getCorpToStoreSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(CorpToStore corpToStore, Hub<CorpToStore> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<CorpToStore> hub) {
    }
}

