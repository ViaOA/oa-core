// Generated by OABuilder
package com.corptostore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.corptostore.delegate.ModelDelegate;
import com.corptostore.model.*;
import com.corptostore.model.oa.*;
import com.corptostore.model.oa.filter.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.resource.Resource;
import com.corptostore.model.oa.Dashboard;
import com.corptostore.model.oa.search.DashboardSearch;
import com.corptostore.model.search.DashboardSearchModel;
import com.viaoa.datasource.*;

public class DashboardSearchModel {
    private static Logger LOG = Logger.getLogger(DashboardSearchModel.class.getName());
    
    protected Hub<Dashboard> hub;  // search results
    protected Hub<Dashboard> hubMultiSelect;
    protected Hub<Dashboard> hubSearchFrom;  // hub (optional) to search from
    protected Hub<DashboardSearch> hubDashboardSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Dashboard> finder;
    
    // object used for search data
    protected DashboardSearch dashboardSearch;
    
    public DashboardSearchModel() {
    }
    
    public DashboardSearchModel(Hub<Dashboard> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Dashboard> getHub() {
        if (hub == null) {
            hub = new Hub<Dashboard>(Dashboard.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Dashboard> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Dashboard> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    DashboardSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Dashboard> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Dashboard.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Dashboard> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Dashboard> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public DashboardSearch getDashboardSearch() {
        if (dashboardSearch != null) return dashboardSearch;
        dashboardSearch = new DashboardSearch();
        return dashboardSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<DashboardSearch> getDashboardSearchHub() {
        if (hubDashboardSearch == null) {
            hubDashboardSearch = new Hub<DashboardSearch>(DashboardSearch.class);
            hubDashboardSearch.add(getDashboardSearch());
            hubDashboardSearch.setPos(0);
        }
        return hubDashboardSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses DashboardSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Dashboard> sel = getDashboardSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Dashboard dashboard, Hub<Dashboard> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Dashboard> hub) {
    }
}

