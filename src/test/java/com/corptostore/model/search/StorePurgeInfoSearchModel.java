// Generated by OABuilder
package com.corptostore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.corptostore.delegate.ModelDelegate;
import com.corptostore.model.*;
import com.corptostore.model.oa.*;
import com.corptostore.model.oa.filter.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.resource.Resource;
import com.corptostore.model.oa.StorePurgeInfo;
import com.corptostore.model.oa.search.StorePurgeInfoSearch;
import com.corptostore.model.search.StorePurgeInfoSearchModel;
import com.viaoa.datasource.*;

public class StorePurgeInfoSearchModel {
    private static Logger LOG = Logger.getLogger(StorePurgeInfoSearchModel.class.getName());
    
    protected Hub<StorePurgeInfo> hub;  // search results
    protected Hub<StorePurgeInfo> hubMultiSelect;
    protected Hub<StorePurgeInfo> hubSearchFrom;  // hub (optional) to search from
    protected Hub<StorePurgeInfoSearch> hubStorePurgeInfoSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, StorePurgeInfo> finder;
    
    // object used for search data
    protected StorePurgeInfoSearch storePurgeInfoSearch;
    
    public StorePurgeInfoSearchModel() {
    }
    
    public StorePurgeInfoSearchModel(Hub<StorePurgeInfo> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<StorePurgeInfo> getHub() {
        if (hub == null) {
            hub = new Hub<StorePurgeInfo>(StorePurgeInfo.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<StorePurgeInfo> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<StorePurgeInfo> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    StorePurgeInfoSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<StorePurgeInfo> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(StorePurgeInfo.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, StorePurgeInfo> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, StorePurgeInfo> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public StorePurgeInfoSearch getStorePurgeInfoSearch() {
        if (storePurgeInfoSearch != null) return storePurgeInfoSearch;
        storePurgeInfoSearch = new StorePurgeInfoSearch();
        return storePurgeInfoSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<StorePurgeInfoSearch> getStorePurgeInfoSearchHub() {
        if (hubStorePurgeInfoSearch == null) {
            hubStorePurgeInfoSearch = new Hub<StorePurgeInfoSearch>(StorePurgeInfoSearch.class);
            hubStorePurgeInfoSearch.add(getStorePurgeInfoSearch());
            hubStorePurgeInfoSearch.setPos(0);
        }
        return hubStorePurgeInfoSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses StorePurgeInfoSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<StorePurgeInfo> sel = getStorePurgeInfoSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(StorePurgeInfo storePurgeInfo, Hub<StorePurgeInfo> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<StorePurgeInfo> hub) {
    }
}

