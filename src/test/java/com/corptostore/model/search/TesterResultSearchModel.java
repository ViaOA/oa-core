// Generated by OABuilder
package com.corptostore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.corptostore.delegate.ModelDelegate;
import com.corptostore.model.*;
import com.corptostore.model.oa.*;
import com.corptostore.model.oa.filter.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.resource.Resource;
import com.corptostore.model.oa.TesterResult;
import com.corptostore.model.oa.search.TesterResultSearch;
import com.corptostore.model.search.TesterResultSearchModel;
import com.viaoa.datasource.*;

public class TesterResultSearchModel {
    private static Logger LOG = Logger.getLogger(TesterResultSearchModel.class.getName());
    
    protected Hub<TesterResult> hub;  // search results
    protected Hub<TesterResult> hubMultiSelect;
    protected Hub<TesterResult> hubSearchFrom;  // hub (optional) to search from
    protected Hub<TesterResultSearch> hubTesterResultSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, TesterResult> finder;
    
    // object used for search data
    protected TesterResultSearch testerResultSearch;
    
    public TesterResultSearchModel() {
    }
    
    public TesterResultSearchModel(Hub<TesterResult> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<TesterResult> getHub() {
        if (hub == null) {
            hub = new Hub<TesterResult>(TesterResult.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<TesterResult> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<TesterResult> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    TesterResultSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<TesterResult> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(TesterResult.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, TesterResult> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, TesterResult> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public TesterResultSearch getTesterResultSearch() {
        if (testerResultSearch != null) return testerResultSearch;
        testerResultSearch = new TesterResultSearch();
        return testerResultSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<TesterResultSearch> getTesterResultSearchHub() {
        if (hubTesterResultSearch == null) {
            hubTesterResultSearch = new Hub<TesterResultSearch>(TesterResultSearch.class);
            hubTesterResultSearch.add(getTesterResultSearch());
            hubTesterResultSearch.setPos(0);
        }
        return hubTesterResultSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses TesterResultSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<TesterResult> sel = getTesterResultSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(TesterResult testerResult, Hub<TesterResult> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<TesterResult> hub) {
    }
}

