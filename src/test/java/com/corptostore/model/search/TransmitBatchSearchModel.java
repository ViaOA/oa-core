// Generated by OABuilder
package com.corptostore.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.corptostore.delegate.ModelDelegate;
import com.corptostore.model.*;
import com.corptostore.model.oa.*;
import com.corptostore.model.oa.filter.*;
import com.corptostore.model.oa.propertypath.*;
import com.corptostore.model.oa.search.*;
import com.corptostore.resource.Resource;
import com.corptostore.model.oa.TransmitBatch;
import com.corptostore.model.oa.search.TransmitBatchSearch;
import com.corptostore.model.search.TransmitBatchSearchModel;
import com.viaoa.datasource.*;

public class TransmitBatchSearchModel {
    private static Logger LOG = Logger.getLogger(TransmitBatchSearchModel.class.getName());
    
    protected Hub<TransmitBatch> hub;  // search results
    protected Hub<TransmitBatch> hubMultiSelect;
    protected Hub<TransmitBatch> hubSearchFrom;  // hub (optional) to search from
    protected Hub<TransmitBatchSearch> hubTransmitBatchSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, TransmitBatch> finder;
    
    // object used for search data
    protected TransmitBatchSearch transmitBatchSearch;
    
    public TransmitBatchSearchModel() {
    }
    
    public TransmitBatchSearchModel(Hub<TransmitBatch> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<TransmitBatch> getHub() {
        if (hub == null) {
            hub = new Hub<TransmitBatch>(TransmitBatch.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<TransmitBatch> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<TransmitBatch> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    TransmitBatchSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<TransmitBatch> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(TransmitBatch.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, TransmitBatch> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, TransmitBatch> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public TransmitBatchSearch getTransmitBatchSearch() {
        if (transmitBatchSearch != null) return transmitBatchSearch;
        transmitBatchSearch = new TransmitBatchSearch();
        return transmitBatchSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<TransmitBatchSearch> getTransmitBatchSearchHub() {
        if (hubTransmitBatchSearch == null) {
            hubTransmitBatchSearch = new Hub<TransmitBatchSearch>(TransmitBatchSearch.class);
            hubTransmitBatchSearch.add(getTransmitBatchSearch());
            hubTransmitBatchSearch.setPos(0);
        }
        return hubTransmitBatchSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses TransmitBatchSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<TransmitBatch> sel = getTransmitBatchSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(TransmitBatch transmitBatch, Hub<TransmitBatch> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<TransmitBatch> hub) {
    }
}

