// Generated by OABuilder
package com.messagedesigner.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.messagedesigner.delegate.ModelDelegate;
import com.messagedesigner.model.*;
import com.messagedesigner.model.oa.*;
import com.messagedesigner.model.oa.filter.*;
import com.messagedesigner.model.oa.propertypath.*;
import com.messagedesigner.model.oa.search.*;
import com.messagedesigner.resource.Resource;
import com.messagedesigner.model.oa.MessageGroup;
import com.messagedesigner.model.oa.search.MessageGroupSearch;
import com.messagedesigner.model.search.MessageGroupSearchModel;
import com.viaoa.datasource.*;

public class MessageGroupSearchModel {
    private static Logger LOG = Logger.getLogger(MessageGroupSearchModel.class.getName());
    
    protected Hub<MessageGroup> hub;  // search results
    protected Hub<MessageGroup> hubMultiSelect;
    protected Hub<MessageGroup> hubSearchFrom;  // hub (optional) to search from
    protected Hub<MessageGroupSearch> hubMessageGroupSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, MessageGroup> finder;
    
    // object used for search data
    protected MessageGroupSearch messageGroupSearch;
    
    public MessageGroupSearchModel() {
    }
    
    public MessageGroupSearchModel(Hub<MessageGroup> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<MessageGroup> getHub() {
        if (hub == null) {
            hub = new Hub<MessageGroup>(MessageGroup.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<MessageGroup> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<MessageGroup> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    MessageGroupSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<MessageGroup> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(MessageGroup.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, MessageGroup> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, MessageGroup> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public MessageGroupSearch getMessageGroupSearch() {
        if (messageGroupSearch != null) return messageGroupSearch;
        messageGroupSearch = new MessageGroupSearch();
        return messageGroupSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<MessageGroupSearch> getMessageGroupSearchHub() {
        if (hubMessageGroupSearch == null) {
            hubMessageGroupSearch = new Hub<MessageGroupSearch>(MessageGroupSearch.class);
            hubMessageGroupSearch.add(getMessageGroupSearch());
            hubMessageGroupSearch.setPos(0);
        }
        return hubMessageGroupSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses MessageGroupSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<MessageGroup> sel = getMessageGroupSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(MessageGroup messageGroup, Hub<MessageGroup> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<MessageGroup> hub) {
    }
}

