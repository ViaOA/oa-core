// Generated by OABuilder
package com.oreillyauto.dev.tool.messagedesigner.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.oreillyauto.dev.tool.messagedesigner.model.*;
import com.oreillyauto.dev.tool.messagedesigner.model.oa.*;
import com.oreillyauto.dev.tool.messagedesigner.model.oa.propertypath.*;
import com.oreillyauto.dev.tool.messagedesigner.model.oa.search.*;
import com.oreillyauto.dev.tool.messagedesigner.model.oa.filter.*;
import com.oreillyauto.dev.tool.messagedesigner.delegate.ModelDelegate;
import com.oreillyauto.dev.tool.messagedesigner.resource.Resource;

public class MessageSourceSearchModel {
    private static Logger LOG = Logger.getLogger(MessageSourceSearchModel.class.getName());
    
    protected Hub<MessageSource> hub;  // search results
    protected Hub<MessageSource> hubMultiSelect;
    protected Hub<MessageSource> hubSearchFrom;  // hub (optional) to search from
    protected Hub<MessageSourceSearch> hubMessageSourceSearch;  // search data, size=1, AO
    // references used in search
    protected Hub<MessageType> hubMessageType;
    
    // finder used to find objects in a path
    protected OAFinder<?, MessageSource> finder;
    
    // ObjectModels
    protected MessageTypeModel modelMessageType;
    
    // SearchModels
    protected MessageTypeSearchModel modelMessageTypeSearch;
    
    // object used for search data
    protected MessageSourceSearch messageSourceSearch;
    
    public MessageSourceSearchModel() {
    }
    
    public MessageSourceSearchModel(Hub<MessageSource> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<MessageSource> getHub() {
        if (hub == null) {
            hub = new Hub<MessageSource>(MessageSource.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<MessageSource> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<MessageSource> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    MessageSourceSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<MessageSource> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(MessageSource.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, MessageSource> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, MessageSource> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public MessageSourceSearch getMessageSourceSearch() {
        if (messageSourceSearch != null) return messageSourceSearch;
        messageSourceSearch = new MessageSourceSearch();
        return messageSourceSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<MessageSourceSearch> getMessageSourceSearchHub() {
        if (hubMessageSourceSearch == null) {
            hubMessageSourceSearch = new Hub<MessageSourceSearch>(MessageSourceSearch.class);
            hubMessageSourceSearch.add(getMessageSourceSearch());
            hubMessageSourceSearch.setPos(0);
        }
        return hubMessageSourceSearch;
    }
    public Hub<MessageType> getMessageTypeHub() {
        if (hubMessageType != null) return hubMessageType;
        hubMessageType = getMessageSourceSearchHub().getDetailHub(MessageSourceSearch.P_MessageType);
        return hubMessageType;
    }
    
    public MessageTypeModel getMessageTypeModel() {
        if (modelMessageType != null) return modelMessageType;
        modelMessageType = new MessageTypeModel(getMessageTypeHub());
        modelMessageType.setDisplayName("Message Types");
        modelMessageType.setPluralDisplayName("Message Types");
        modelMessageType.setAllowNew(false);
        modelMessageType.setAllowSave(true);
        modelMessageType.setAllowAdd(false);
        modelMessageType.setAllowRemove(false);
        modelMessageType.setAllowClear(true);
        modelMessageType.setAllowDelete(false);
        modelMessageType.setAllowSearch(true);
        modelMessageType.setAllowHubSearch(false);
        modelMessageType.setAllowGotoEdit(true);
        return modelMessageType;
    }
    
    public MessageTypeSearchModel getMessageTypeSearchModel() {
        if (modelMessageTypeSearch == null) {
            modelMessageTypeSearch = new MessageTypeSearchModel();
            getMessageSourceSearch().setMessageTypeSearch(modelMessageTypeSearch.getMessageTypeSearch());
        }
        return modelMessageTypeSearch;
    }
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses MessageSourceSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<MessageSource> sel = getMessageSourceSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(MessageSource messageSource, Hub<MessageSource> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<MessageSource> hub) {
    }
}

