// Generated by OABuilder
package com.oreillyauto.remodel.model.oa.search;

import java.util.logging.*;
import com.oreillyauto.remodel.model.oa.*;
import com.oreillyauto.remodel.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.OADateTime;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ColumnSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(ColumnSearch.class.getName());
    public static final String P_Name = "Name";
    public static final String P_NewName = "NewName";
    public static final String P_Created = "Created";
    public static final String P_Created2 = "Created2";
    public static final String P_Table = "Table";
    public static final String P_UseTableSearch = "UseTableSearch";
    public static final String P_MaxResults = "MaxResults";

    protected String name;
    protected String newName;
    protected OADateTime created;
    protected OADateTime created2;
    protected Table table;
    protected boolean useTableSearch;
    protected TableSearch searchTable;
    protected int maxResults;

    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      

    public String getNewName() {
        return newName;
    }
    public void setNewName(String newValue) {
        String old = newName;
        fireBeforePropertyChange(P_NewName, old, newValue);
        this.newName = newValue;
        firePropertyChange(P_NewName, old, this.newName);
    }
      

    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
        if (isLoading()) return;
        if (created != null) {
            if (created2 == null) setCreated2(this.created.addDays(1));
            else if (created.compareTo(created2) > 0) setCreated2(this.created.addDays(1));
        }
    } 
    public OADateTime getCreated2() {
        return created2;
    }
    public void setCreated2(OADateTime newValue) {
        OADateTime old = created2;
        fireBeforePropertyChange(P_Created2, old, newValue);
        this.created2 = newValue;
        firePropertyChange(P_Created2, old, this.created2);
        if (created != null && created2 != null) {
            if (created.compareTo(created2) > 0) setCreated(this.created2);
        }
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    @OAOne
    public Table getTable() {
        if (table == null) {
            table = (Table) getObject(P_Table);
        }
        return table;
    }
    public void setTable(Table newValue) {
        Table old = this.table;
        this.table = newValue;
        firePropertyChange(P_Table, old, this.table);
    }
    public boolean getUseTableSearch() {
        return useTableSearch;
    }
    public void setUseTableSearch(boolean newValue) {
        boolean old = this.useTableSearch;
        this.useTableSearch = newValue;
        firePropertyChange(P_UseTableSearch, old, this.useTableSearch);
    }
    public TableSearch getTableSearch() {
        return this.searchTable;
    }
    public void setTableSearch(TableSearch newValue) {
        this.searchTable = newValue;
    }

    public void reset() {
        setName(null);
        setNewName(null);
        setCreated(null);
        setCreated2(null);
        setTable(null);
        setUseTableSearch(false);
    }

    public boolean isDataEntered() {
        if (getName() != null) return true;
        if (getNewName() != null) return true;
        if (getCreated() != null) return true;
        if (getTable() != null) return true;
        if (getUseTableSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<Column> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<Column> f = new OAQueryFilter<Column>(Column.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<Column> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<Column> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<Column> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        OAFinder finder = null;
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(name);
            if (value.indexOf("%") >= 0) {
                sql += Column.P_Name + " LIKE ?";
            }
            else {
                sql += Column.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (OAString.isNotEmpty(this.newName)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(newName);
            if (value.indexOf("%") >= 0) {
                sql += Column.P_NewName + " LIKE ?";
            }
            else {
                sql += Column.P_NewName + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            if (created2 != null && !created.equals(created2)) {
                sql += Column.P_Created + " >= ?";
                args = OAArray.add(Object.class, args, this.created);
                sql += " AND " + Column.P_Created + " <= ?";
                args = OAArray.add(Object.class, args, this.created2);
            }
            else {
                sql += Column.P_Created + " = ?";
                args = OAArray.add(Object.class, args, this.created);
            }
        }
        if (!useTableSearch && getTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ColumnPP.table().pp + " = ?";
            args = OAArray.add(Object.class, args, getTable());
            finder = new OAFinder<Table, Column>(getTable(), Table.P_Columns);
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<Column> select = new OASelect<Column>(Column.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFinder(finder);
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useTableSearch && getTableSearch() != null) {
            getTableSearch().appendSelect(ColumnPP.table().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(name);
            if (value.indexOf("%") >= 0) {
                sql += prefix + Column.P_Name + " LIKE ?";
            }
            else {
                sql += prefix + Column.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (OAString.isNotEmpty(this.newName)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(newName);
            if (value.indexOf("%") >= 0) {
                sql += prefix + Column.P_NewName + " LIKE ?";
            }
            else {
                sql += prefix + Column.P_NewName + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (created != null) {
            if (sql.length() > 0) sql += " AND ";
            if (created2 != null && !created.equals(created2)) {
                sql += prefix + Column.P_Created + " >= ?";
                args = OAArray.add(Object.class, args, this.created);
                sql += " AND " + prefix + Column.P_Created + " <= ?";
                args = OAArray.add(Object.class, args, this.created2);
            }
            else {
                sql += prefix + Column.P_Created + " = ?";
                args = OAArray.add(Object.class, args, this.created);
            }
        }
        if (!useTableSearch && getTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ColumnPP.table().pp + " = ?";
            args = OAArray.add(Object.class, args, getTable());
        }
        if (useTableSearch && getTableSearch() != null) {
            getTableSearch().appendSelect(prefix + ColumnPP.table().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<Column> filterDataSourceFilter;
    public OAFilter<Column> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<Column>() {
            @Override
            public boolean isUsed(Column column) {
                return ColumnSearch.this.isUsedForDataSourceFilter(column);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<Column> filterCustomFilter;
    public OAFilter<Column> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<Column>() {
            @Override
            public boolean isUsed(Column column) {
                boolean b = ColumnSearch.this.isUsedForCustomFilter(column);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(Column searchColumn) {
        return true;
    }
    public boolean isUsedForCustomFilter(Column searchColumn) {
        return true;
    }
}
