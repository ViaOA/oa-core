// Generated by OABuilder
package com.oreillyauto.remodel.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.oreillyauto.remodel.model.*;
import com.oreillyauto.remodel.model.oa.*;
import com.oreillyauto.remodel.model.oa.propertypath.*;
import com.oreillyauto.remodel.model.oa.search.*;
import com.oreillyauto.remodel.model.oa.filter.*;
import com.oreillyauto.remodel.delegate.ModelDelegate;
import com.oreillyauto.remodel.resource.Resource;

public class QueryColumnSearchModel {
    private static Logger LOG = Logger.getLogger(QueryColumnSearchModel.class.getName());
    
    protected Hub<QueryColumn> hub;  // search results
    protected Hub<QueryColumn> hubMultiSelect;
    protected Hub<QueryColumn> hubSearchFrom;  // hub (optional) to search from
    protected Hub<QueryColumnSearch> hubQueryColumnSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, QueryColumn> finder;
    
    // object used for search data
    protected QueryColumnSearch queryColumnSearch;
    
    public QueryColumnSearchModel() {
    }
    
    public QueryColumnSearchModel(Hub<QueryColumn> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<QueryColumn> getHub() {
        if (hub == null) {
            hub = new Hub<QueryColumn>(QueryColumn.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<QueryColumn> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<QueryColumn> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    QueryColumnSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<QueryColumn> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(QueryColumn.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, QueryColumn> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, QueryColumn> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public QueryColumnSearch getQueryColumnSearch() {
        if (queryColumnSearch != null) return queryColumnSearch;
        queryColumnSearch = new QueryColumnSearch();
        return queryColumnSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<QueryColumnSearch> getQueryColumnSearchHub() {
        if (hubQueryColumnSearch == null) {
            hubQueryColumnSearch = new Hub<QueryColumnSearch>(QueryColumnSearch.class);
            hubQueryColumnSearch.add(getQueryColumnSearch());
            hubQueryColumnSearch.setPos(0);
        }
        return hubQueryColumnSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses QueryColumnSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<QueryColumn> sel = getQueryColumnSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(QueryColumn queryColumn, Hub<QueryColumn> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<QueryColumn> hub) {
    }
}

