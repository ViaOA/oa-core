// Generated by OABuilder
package com.oreillyauto.remodel.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.oreillyauto.remodel.model.*;
import com.oreillyauto.remodel.model.oa.*;
import com.oreillyauto.remodel.model.oa.propertypath.*;
import com.oreillyauto.remodel.model.oa.search.*;
import com.oreillyauto.remodel.model.oa.filter.*;
import com.oreillyauto.remodel.delegate.ModelDelegate;
import com.oreillyauto.remodel.resource.Resource;

public class QueryInfoSearchModel {
    private static Logger LOG = Logger.getLogger(QueryInfoSearchModel.class.getName());
    
    protected Hub<QueryInfo> hub;  // search results
    protected Hub<QueryInfo> hubMultiSelect;
    protected Hub<QueryInfo> hubSearchFrom;  // hub (optional) to search from
    protected Hub<QueryInfoSearch> hubQueryInfoSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, QueryInfo> finder;
    
    // object used for search data
    protected QueryInfoSearch queryInfoSearch;
    
    public QueryInfoSearchModel() {
    }
    
    public QueryInfoSearchModel(Hub<QueryInfo> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<QueryInfo> getHub() {
        if (hub == null) {
            hub = new Hub<QueryInfo>(QueryInfo.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<QueryInfo> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<QueryInfo> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    QueryInfoSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<QueryInfo> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(QueryInfo.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, QueryInfo> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, QueryInfo> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public QueryInfoSearch getQueryInfoSearch() {
        if (queryInfoSearch != null) return queryInfoSearch;
        queryInfoSearch = new QueryInfoSearch();
        return queryInfoSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<QueryInfoSearch> getQueryInfoSearchHub() {
        if (hubQueryInfoSearch == null) {
            hubQueryInfoSearch = new Hub<QueryInfoSearch>(QueryInfoSearch.class);
            hubQueryInfoSearch.add(getQueryInfoSearch());
            hubQueryInfoSearch.setPos(0);
        }
        return hubQueryInfoSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses QueryInfoSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<QueryInfo> sel = getQueryInfoSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(QueryInfo queryInfo, Hub<QueryInfo> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<QueryInfo> hub) {
    }
}

