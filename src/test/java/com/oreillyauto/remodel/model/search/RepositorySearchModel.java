// Generated by OABuilder
package com.oreillyauto.remodel.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.oreillyauto.remodel.model.*;
import com.oreillyauto.remodel.model.oa.*;
import com.oreillyauto.remodel.model.oa.propertypath.*;
import com.oreillyauto.remodel.model.oa.search.*;
import com.oreillyauto.remodel.model.oa.filter.*;
import com.oreillyauto.remodel.delegate.ModelDelegate;
import com.oreillyauto.remodel.resource.Resource;

public class RepositorySearchModel {
    private static Logger LOG = Logger.getLogger(RepositorySearchModel.class.getName());
    
    protected Hub<Repository> hub;  // search results
    protected Hub<Repository> hubMultiSelect;
    protected Hub<Repository> hubSearchFrom;  // hub (optional) to search from
    protected Hub<RepositorySearch> hubRepositorySearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Repository> finder;
    
    // object used for search data
    protected RepositorySearch repositorySearch;
    
    public RepositorySearchModel() {
    }
    
    public RepositorySearchModel(Hub<Repository> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Repository> getHub() {
        if (hub == null) {
            hub = new Hub<Repository>(Repository.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Repository> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Repository> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    RepositorySearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Repository> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Repository.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Repository> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Repository> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public RepositorySearch getRepositorySearch() {
        if (repositorySearch != null) return repositorySearch;
        repositorySearch = new RepositorySearch();
        return repositorySearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<RepositorySearch> getRepositorySearchHub() {
        if (hubRepositorySearch == null) {
            hubRepositorySearch = new Hub<RepositorySearch>(RepositorySearch.class);
            hubRepositorySearch.add(getRepositorySearch());
            hubRepositorySearch.setPos(0);
        }
        return hubRepositorySearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses RepositorySearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Repository> sel = getRepositorySearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Repository repository, Hub<Repository> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Repository> hub) {
    }
}

