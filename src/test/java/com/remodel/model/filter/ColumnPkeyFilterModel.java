// Generated by OABuilder
package com.remodel.model.filter;

import java.util.logging.*;

import com.viaoa.object.*;
import com.remodel.delegate.ModelDelegate;
import com.remodel.model.*;
import com.remodel.model.oa.*;
import com.remodel.model.oa.filter.*;
import com.remodel.model.oa.propertypath.*;
import com.remodel.model.search.*;
import com.remodel.resource.Resource;
import com.viaoa.annotation.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.datasource.*;

public class ColumnPkeyFilterModel {
    private static Logger LOG = Logger.getLogger(ColumnPkeyFilterModel.class.getName());
    
    // Hubs
    protected Hub<ColumnPkeyFilter> hubFilter;
    
    // ObjectModels
    
    // object used for filter data
    protected ColumnPkeyFilter filter;
    
    public ColumnPkeyFilterModel(Hub<Column> hubMaster, Hub<Column> hub) {
        filter = new ColumnPkeyFilter(hubMaster, hub);
    }
    public ColumnPkeyFilterModel(Hub<Column> hub) {
        filter = new ColumnPkeyFilter(hub);
    }
    
    // object used to input query data, to be used by filterHub
    public ColumnPkeyFilter getFilter() {
        return filter;
    }
    
    // hub for filter UI object - used to bind with UI components for entering filter data
    public Hub<ColumnPkeyFilter> getFilterHub() {
        if (hubFilter == null) {
            hubFilter = new Hub<ColumnPkeyFilter>(ColumnPkeyFilter.class);
            hubFilter.add(getFilter());
            hubFilter.setPos(0);
        }
        return hubFilter;
    }
    
    
    
    // get the Filtered hub
    public Hub<Column> getHub() {
        return getFilter().getHubFilter().getHub();
    }
}

