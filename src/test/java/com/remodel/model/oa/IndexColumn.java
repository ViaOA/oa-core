// Generated by OABuilder
package com.remodel.model.oa;
 
import java.util.*;
import java.util.logging.*;
import java.sql.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.scheduler.*;
import com.viaoa.util.*;
import com.remodel.delegate.oa.*;
import com.remodel.model.oa.filter.*;
import com.remodel.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.util.OADateTime;
 
@OAClass(
    lowerName = "indexColumn",
    pluralName = "IndexColumns",
    shortName = "inc",
    displayName = "Index Column",
    displayProperty = "columnName"
)
@OATable(
    indexes = {
        @OAIndex(name = "IndexColumnColumn", fkey = true, columns = { @OAIndexColumn(name = "ColumnId") }), 
        @OAIndex(name = "IndexColumnIndex", fkey = true, columns = { @OAIndexColumn(name = "IndexId") })
    }
)
public class IndexColumn extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(IndexColumn.class.getName());

    public static final String P_Id = "Id";
    public static final String P_Created = "Created";
    public static final String P_ColumnName = "ColumnName";
    public static final String P_Seq = "Seq";
     
     
    public static final String P_Column = "Column";
    public static final String P_Index = "Index";
     
    protected volatile int id;
    protected volatile OADateTime created;
    protected volatile String columnName;
    protected volatile int seq;
     
    // Links to other objects.
    protected volatile transient Column column;
    protected volatile transient Index index;
     
    public IndexColumn() {
        if (!isLoading()) setObjectDefaults();
    }
    @Override
    public void setObjectDefaults() {
        setCreated(new OADateTime());
    }
     
    public IndexColumn(int id) {
        this();
        setId(id);
    }
     

    @OAProperty(isUnique = true, trackPrimitiveNull = false, displayLength = 6)
    @OAId()
    @OAColumn(sqlType = java.sql.Types.INTEGER)
    public int getId() {
        return id;
    }
    public void setId(int newValue) {
        int old = id;
        fireBeforePropertyChange(P_Id, old, newValue);
        this.id = newValue;
        firePropertyChange(P_Id, old, this.id);
    }
    @OAProperty(defaultValue = "new OADateTime()", displayLength = 15, isProcessed = true)
    @OAColumn(sqlType = java.sql.Types.TIMESTAMP)
    public OADateTime getCreated() {
        return created;
    }
    public void setCreated(OADateTime newValue) {
        OADateTime old = created;
        fireBeforePropertyChange(P_Created, old, newValue);
        this.created = newValue;
        firePropertyChange(P_Created, old, this.created);
    }
    @OAProperty(displayName = "Column Name", maxLength = 250, displayLength = 20)
    @OAColumn(maxLength = 250)
    public String getColumnName() {
        return columnName;
    }
    public void setColumnName(String newValue) {
        String old = columnName;
        fireBeforePropertyChange(P_ColumnName, old, newValue);
        this.columnName = newValue;
        firePropertyChange(P_ColumnName, old, this.columnName);
    }
    @OAProperty(displayLength = 6, isAutoSeq = true)
    @OAColumn(sqlType = java.sql.Types.INTEGER)
    public int getSeq() {
        return seq;
    }
    public void setSeq(int newValue) {
        int old = seq;
        fireBeforePropertyChange(P_Seq, old, newValue);
        this.seq = newValue;
        firePropertyChange(P_Seq, old, this.seq);
    }
    @OAOne(
        reverseName = Column.P_IndexColumns, 
        allowCreateNew = false
    )
    @OAFkey(columns = {"ColumnId"})
    public Column getColumn() {
        if (column == null) {
            column = (Column) getObject(P_Column);
        }
        return column;
    }
    public void setColumn(Column newValue) {
        Column old = this.column;
        fireBeforePropertyChange(P_Column, old, newValue);
        this.column = newValue;
        firePropertyChange(P_Column, old, this.column);
    }
    @OAOne(
        reverseName = Index.P_IndexColumns, 
        required = true, 
        allowCreateNew = false
    )
    @OAFkey(columns = {"IndexId"})
    public Index getIndex() {
        if (index == null) {
            index = (Index) getObject(P_Index);
        }
        return index;
    }
    public void setIndex(Index newValue) {
        Index old = this.index;
        fireBeforePropertyChange(P_Index, old, newValue);
        this.index = newValue;
        firePropertyChange(P_Index, old, this.index);
    }
    public void load(ResultSet rs, int id) throws SQLException {
        this.id = id;
        java.sql.Timestamp timestamp;
        timestamp = rs.getTimestamp(2);
        if (timestamp != null) this.created = new OADateTime(timestamp);
        this.columnName = rs.getString(3);
        this.seq = (int) rs.getInt(4);
        if (rs.wasNull()) {
            OAObjectInfoDelegate.setPrimitiveNull(this, IndexColumn.P_Seq, true);
        }
        int columnFkey = rs.getInt(5);
        if (!rs.wasNull() && columnFkey > 0) {
            setProperty(P_Column, new OAObjectKey(columnFkey));
        }
        int indexFkey = rs.getInt(6);
        if (!rs.wasNull() && indexFkey > 0) {
            setProperty(P_Index, new OAObjectKey(indexFkey));
        }
        if (rs.getMetaData().getColumnCount() != 6) {
            throw new SQLException("invalid number of columns for load method");
        }

        this.changedFlag = false;
        this.newFlag = false;
    }
}
 
