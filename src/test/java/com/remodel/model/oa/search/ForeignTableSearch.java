// Generated by OABuilder
package com.remodel.model.oa.search;

import java.util.logging.*;

import com.remodel.model.oa.*;
import com.remodel.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class ForeignTableSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(ForeignTableSearch.class.getName());
    public static final String P_Table = "Table";
    public static final String P_UseTableSearch = "UseTableSearch";
    public static final String P_ToTable = "ToTable";
    public static final String P_UseToTableSearch = "UseToTableSearch";
    public static final String P_MaxResults = "MaxResults";

    protected Table table;
    protected boolean useTableSearch;
    protected TableSearch searchTable;
    protected Table toTable;
    protected boolean useToTableSearch;
    protected TableSearch searchToTable;
    protected int maxResults;

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    @OAOne
    public Table getTable() {
        if (table == null) {
            table = (Table) getObject(P_Table);
        }
        return table;
    }
    public void setTable(Table newValue) {
        Table old = this.table;
        this.table = newValue;
        firePropertyChange(P_Table, old, this.table);
    }
    public boolean getUseTableSearch() {
        return useTableSearch;
    }
    public void setUseTableSearch(boolean newValue) {
        boolean old = this.useTableSearch;
        this.useTableSearch = newValue;
        firePropertyChange(P_UseTableSearch, old, this.useTableSearch);
    }
    public TableSearch getTableSearch() {
        return this.searchTable;
    }
    public void setTableSearch(TableSearch newValue) {
        this.searchTable = newValue;
    }

    @OAOne
    public Table getToTable() {
        if (toTable == null) {
            toTable = (Table) getObject(P_ToTable);
        }
        return toTable;
    }
    public void setToTable(Table newValue) {
        Table old = this.toTable;
        this.toTable = newValue;
        firePropertyChange(P_ToTable, old, this.toTable);
    }
    public boolean getUseToTableSearch() {
        return useToTableSearch;
    }
    public void setUseToTableSearch(boolean newValue) {
        boolean old = this.useToTableSearch;
        this.useToTableSearch = newValue;
        firePropertyChange(P_UseToTableSearch, old, this.useToTableSearch);
    }
    public TableSearch getToTableSearch() {
        return this.searchToTable;
    }
    public void setToTableSearch(TableSearch newValue) {
        this.searchToTable = newValue;
    }

    public void reset() {
        setTable(null);
        setUseTableSearch(false);
        setToTable(null);
        setUseToTableSearch(false);
    }

    public boolean isDataEntered() {
        if (getTable() != null) return true;
        if (getUseTableSearch()) return true;
        if (getToTable() != null) return true;
        if (getUseToTableSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ForeignTable> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ForeignTable> f = new OAQueryFilter<ForeignTable>(ForeignTable.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ForeignTable> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ForeignTable> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ForeignTable> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        OAFinder finder = null;
        if (!useTableSearch && getTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ForeignTablePP.table().pp + " = ?";
            args = OAArray.add(Object.class, args, getTable());
            finder = new OAFinder<Table, ForeignTable>(getTable(), Table.P_ForeignTables);
        }
        if (!useToTableSearch && getToTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += ForeignTablePP.toTable().pp + " = ?";
            args = OAArray.add(Object.class, args, getToTable());
            finder = new OAFinder<Table, ForeignTable>(getToTable(), Table.P_TableForeignTables);
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ForeignTable> select = new OASelect<ForeignTable>(ForeignTable.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        select.setFinder(finder);
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useTableSearch && getTableSearch() != null) {
            getTableSearch().appendSelect(ForeignTablePP.table().pp, select);
        }
        if (useToTableSearch && getToTableSearch() != null) {
            getToTableSearch().appendSelect(ForeignTablePP.toTable().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (!useTableSearch && getTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ForeignTablePP.table().pp + " = ?";
            args = OAArray.add(Object.class, args, getTable());
        }
        if (useTableSearch && getTableSearch() != null) {
            getTableSearch().appendSelect(prefix + ForeignTablePP.table().pp, select);
        }
        if (!useToTableSearch && getToTable() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + ForeignTablePP.toTable().pp + " = ?";
            args = OAArray.add(Object.class, args, getToTable());
        }
        if (useToTableSearch && getToTableSearch() != null) {
            getToTableSearch().appendSelect(prefix + ForeignTablePP.toTable().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<ForeignTable> filterDataSourceFilter;
    public OAFilter<ForeignTable> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ForeignTable>() {
            @Override
            public boolean isUsed(ForeignTable foreignTable) {
                return ForeignTableSearch.this.isUsedForDataSourceFilter(foreignTable);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ForeignTable> filterCustomFilter;
    public OAFilter<ForeignTable> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ForeignTable>() {
            @Override
            public boolean isUsed(ForeignTable foreignTable) {
                boolean b = ForeignTableSearch.this.isUsedForCustomFilter(foreignTable);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ForeignTable searchForeignTable) {
        return true;
    }
    public boolean isUsedForCustomFilter(ForeignTable searchForeignTable) {
        return true;
    }
}
