// Generated by OABuilder
package com.remodel.model.oa.search;

import java.util.logging.*;

import com.remodel.model.oa.*;
import com.remodel.model.oa.propertypath.*;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.datasource.*;
import com.viaoa.filter.*;

@OAClass(useDataSource=false, localOnly=true)
public class SqlTypeSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(SqlTypeSearch.class.getName());
    public static final String P_Name = "Name";
    public static final String P_SqlType = "SqlType";
    public static final String P_SqlType2 = "SqlType2";
    public static final String P_DataType = "DataType";
    public static final String P_UseDataTypeSearch = "UseDataTypeSearch";
    public static final String P_MaxResults = "MaxResults";

    protected String name;
    protected int sqlType;
    protected int sqlType2;
    protected DataType dataType;
    protected boolean useDataTypeSearch;
    protected DataTypeSearch searchDataType;
    protected int maxResults;

    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        String old = name;
        fireBeforePropertyChange(P_Name, old, newValue);
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
      

    public int getSqlType() {
        return sqlType;
    }
    public void setSqlType(int newValue) {
        int old = sqlType;
        fireBeforePropertyChange(P_SqlType, old, newValue);
        this.sqlType = newValue;
        firePropertyChange(P_SqlType, old, this.sqlType);
        if (isLoading()) return;
        if (sqlType > sqlType2) setSqlType2(this.sqlType);
    } 
    public int getSqlType2() {
        return sqlType2;
    }
    public void setSqlType2(int newValue) {
        int old = sqlType2;
        fireBeforePropertyChange(P_SqlType2, old, newValue);
        this.sqlType2 = newValue;
        firePropertyChange(P_SqlType2, old, this.sqlType2);
        if (isLoading()) return;
        if (sqlType > sqlType2) setSqlType(this.sqlType2);
    }

    public int getMaxResults() {
        return maxResults;
    }
    public void setMaxResults(int newValue) {
        fireBeforePropertyChange(P_MaxResults, this.maxResults, newValue);
        int old = maxResults;
        this.maxResults = newValue;
        firePropertyChange(P_MaxResults, old, this.maxResults);
    }

    @OAOne
    public DataType getDataType() {
        if (dataType == null) {
            dataType = (DataType) getObject(P_DataType);
        }
        return dataType;
    }
    public void setDataType(DataType newValue) {
        DataType old = this.dataType;
        this.dataType = newValue;
        firePropertyChange(P_DataType, old, this.dataType);
    }
    public boolean getUseDataTypeSearch() {
        return useDataTypeSearch;
    }
    public void setUseDataTypeSearch(boolean newValue) {
        boolean old = this.useDataTypeSearch;
        this.useDataTypeSearch = newValue;
        firePropertyChange(P_UseDataTypeSearch, old, this.useDataTypeSearch);
    }
    public DataTypeSearch getDataTypeSearch() {
        return this.searchDataType;
    }
    public void setDataTypeSearch(DataTypeSearch newValue) {
        this.searchDataType = newValue;
    }

    public void reset() {
        setName(null);
        setSqlType(0);
        setNull(P_SqlType);
        setSqlType2(0);
        setNull(P_SqlType2);
        setDataType(null);
        setUseDataTypeSearch(false);
    }

    public boolean isDataEntered() {
        if (getName() != null) return true;
        if (!isNull(P_SqlType)) return true;
        if (getDataType() != null) return true;
        if (getUseDataTypeSearch()) return true;
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<SqlType> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (OAString.isNotEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<SqlType> f = new OAQueryFilter<SqlType>(SqlType.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<SqlType> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<SqlType> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<SqlType> getSelect() {
        final String prefix = "";
        String sql = "";
        String sortOrder = null;
        Object[] args = new Object[0];
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(name);
            if (value.indexOf("%") >= 0) {
                sql += SqlType.P_Name + " LIKE ?";
            }
            else {
                sql += SqlType.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!isNull(P_SqlType)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_SqlType2) && sqlType != sqlType2) {
                sql += SqlType.P_SqlType + " >= ?";
                args = OAArray.add(Object.class, args, getSqlType());
                sql += " AND " + SqlType.P_SqlType + " <= ?";
                args = OAArray.add(Object.class, args, getSqlType2());
            }
            else {
                sql += SqlType.P_SqlType + " = ?";
                args = OAArray.add(Object.class, args, getSqlType());
            }
        }
        if (!useDataTypeSearch && getDataType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += SqlTypePP.dataType().pp + " = ?";
            args = OAArray.add(Object.class, args, getDataType());
        }

        if (OAString.isNotEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<SqlType> select = new OASelect<SqlType>(SqlType.class, sql, args, sortOrder);
        if (getExtraWhereFilter() != null && getExtraWhereFilter().updateSelect(select)) {
            select.setFilter(new OAAndFilter(this.getCustomFilter(), getExtraWhereFilter()));
        }
        else select.setFilter(this.getCustomFilter());
        select.setDataSourceFilter(this.getDataSourceFilter());
        if (getMaxResults() > 0) select.setMax(getMaxResults());
        if (useDataTypeSearch && getDataTypeSearch() != null) {
            getDataTypeSearch().appendSelect(SqlTypePP.dataType().pp, select);
        }
        return select;
    }

    public void appendSelect(final String fromName, final OASelect select) {
        final String prefix = fromName + ".";
        String sql = "";
        Object[] args = new Object[0];
        if (OAString.isNotEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = OAString.convertToLikeSearch(name);
            if (value.indexOf("%") >= 0) {
                sql += prefix + SqlType.P_Name + " LIKE ?";
            }
            else {
                sql += prefix + SqlType.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!isNull(P_SqlType)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_SqlType2) && sqlType != sqlType2) {
                sql += prefix + SqlType.P_SqlType + " >= ?";
                args = OAArray.add(Object.class, args, getSqlType());
                sql += " AND " + prefix + SqlType.P_SqlType + " <= ?";
                args = OAArray.add(Object.class, args, getSqlType2());
            }
            else {
                sql += prefix + SqlType.P_SqlType + " = ?";
                args = OAArray.add(Object.class, args, getSqlType());
            }
        }
        if (!useDataTypeSearch && getDataType() != null) {
            if (sql.length() > 0) sql += " AND ";
            sql += prefix + SqlTypePP.dataType().pp + " = ?";
            args = OAArray.add(Object.class, args, getDataType());
        }
        if (useDataTypeSearch && getDataTypeSearch() != null) {
            getDataTypeSearch().appendSelect(prefix + SqlTypePP.dataType().pp, select);
        }
        select.add(sql, args);
    }

    private OAFilter<SqlType> filterDataSourceFilter;
    public OAFilter<SqlType> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<SqlType>() {
            @Override
            public boolean isUsed(SqlType sqlType) {
                return SqlTypeSearch.this.isUsedForDataSourceFilter(sqlType);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<SqlType> filterCustomFilter;
    public OAFilter<SqlType> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<SqlType>() {
            @Override
            public boolean isUsed(SqlType sqlType) {
                boolean b = SqlTypeSearch.this.isUsedForCustomFilter(sqlType);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(SqlType searchSqlType) {
        return true;
    }
    public boolean isUsedForCustomFilter(SqlType searchSqlType) {
        return true;
    }
}
