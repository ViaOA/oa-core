// Generated by OABuilder
package com.remodel.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.remodel.delegate.ModelDelegate;
import com.remodel.model.*;
import com.remodel.model.oa.*;
import com.remodel.model.oa.filter.*;
import com.remodel.model.oa.propertypath.*;
import com.remodel.model.oa.search.*;
import com.remodel.resource.Resource;
import com.viaoa.datasource.*;

public class ColumnTypeSearchModel {
    private static Logger LOG = Logger.getLogger(ColumnTypeSearchModel.class.getName());
    
    protected Hub<ColumnType> hub;  // search results
    protected Hub<ColumnType> hubMultiSelect;
    protected Hub<ColumnType> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ColumnTypeSearch> hubColumnTypeSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ColumnType> finder;
    
    // object used for search data
    protected ColumnTypeSearch columnTypeSearch;
    
    public ColumnTypeSearchModel() {
    }
    
    public ColumnTypeSearchModel(Hub<ColumnType> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ColumnType> getHub() {
        if (hub == null) {
            hub = new Hub<ColumnType>(ColumnType.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ColumnType> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ColumnType> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ColumnTypeSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ColumnType> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ColumnType.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ColumnType> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ColumnType> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ColumnTypeSearch getColumnTypeSearch() {
        if (columnTypeSearch != null) return columnTypeSearch;
        columnTypeSearch = new ColumnTypeSearch();
        return columnTypeSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ColumnTypeSearch> getColumnTypeSearchHub() {
        if (hubColumnTypeSearch == null) {
            hubColumnTypeSearch = new Hub<ColumnTypeSearch>(ColumnTypeSearch.class);
            hubColumnTypeSearch.add(getColumnTypeSearch());
            hubColumnTypeSearch.setPos(0);
        }
        return hubColumnTypeSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ColumnTypeSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ColumnType> sel = getColumnTypeSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ColumnType columnType, Hub<ColumnType> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ColumnType> hub) {
    }
}

