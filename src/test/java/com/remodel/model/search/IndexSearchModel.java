// Generated by OABuilder
package com.remodel.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.remodel.delegate.ModelDelegate;
import com.remodel.model.*;
import com.remodel.model.oa.*;
import com.remodel.model.oa.filter.*;
import com.remodel.model.oa.propertypath.*;
import com.remodel.model.oa.search.*;
import com.remodel.resource.Resource;
import com.viaoa.datasource.*;

public class IndexSearchModel {
    private static Logger LOG = Logger.getLogger(IndexSearchModel.class.getName());
    
    protected Hub<Index> hub;  // search results
    protected Hub<Index> hubMultiSelect;
    protected Hub<Index> hubSearchFrom;  // hub (optional) to search from
    protected Hub<IndexSearch> hubIndexSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, Index> finder;
    
    // object used for search data
    protected IndexSearch indexSearch;
    
    public IndexSearchModel() {
    }
    
    public IndexSearchModel(Hub<Index> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<Index> getHub() {
        if (hub == null) {
            hub = new Hub<Index>(Index.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<Index> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<Index> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    IndexSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<Index> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(Index.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, Index> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, Index> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public IndexSearch getIndexSearch() {
        if (indexSearch != null) return indexSearch;
        indexSearch = new IndexSearch();
        return indexSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<IndexSearch> getIndexSearchHub() {
        if (hubIndexSearch == null) {
            hubIndexSearch = new Hub<IndexSearch>(IndexSearch.class);
            hubIndexSearch.add(getIndexSearch());
            hubIndexSearch.setPos(0);
        }
        return hubIndexSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses IndexSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<Index> sel = getIndexSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(Index index, Hub<Index> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<Index> hub) {
    }
}

